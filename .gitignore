# dependencies
node_modules/
.pnpm-store/

# build output
dist/
build/
target/
.cache/
*.tsbuildinfo

# NOTE: generated code (packages/sdk-ts/src/gen, crates/jb-core/src/gen,
# tools/vectors/gen) IS committed on purpose. AR-10/INF-34 require CI to regenerate and
# diff, which only detects a hand edit if the previous output is in the tree. It also
# means a fresh clone builds without protoc installed. Never hand-edit it (L-01).

# expo / react native
.expo/
.expo-shared/
frontend/android/
frontend/ios/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*

# turbo
.turbo/

# test / coverage
coverage/
.nyc_output/
__pycache__/
.pytest_cache/
.venv/

# secrets and local config — never commit a server key
*.env
.env.*
!.env.example
ops/secrets/
*.pem
server.key
# Per-deployment tunnel/NAT port map. Generated by `just publish-all`; the committed
# reference is ops/service-map.json.example.
ops/service-map.json

# editors / os
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# local execution state
.run/


rns-chaquopy-reference/
# Generated by backend/src/cli/scale-gen.ts (L-01: regenerate, never hand-edit)
ops/scale-compose.generated.yml

# Measurement output — reproducible via `pnpm scale:measure`
ops/results/

# Local simplified paper rewrite
/simplified-introduction-background-research-gap.txt
