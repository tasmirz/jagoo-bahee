# Metro (React Native) cannot resolve pnpm's symlinked node_modules layout.
# Hoisting is the Expo-documented workaround and is required for `frontend/` to bundle.
# Build log L-04.
node-linker=hoisted

# Keep peer resolution strict everywhere else so version drift surfaces at install time.
strict-peer-dependencies=false
auto-install-peers=true
