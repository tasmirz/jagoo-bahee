// Driven adapters: Mongo envelope/projection store, Redis credits/nullifiers/cache, Merkle witness.
// Each implements a core/ports abstract class and ships an in-memory double alongside it. Populated in P1.
export {};
