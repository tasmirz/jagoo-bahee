// One directory per feature (CLAUDE.md §5.4): handlers, projections, read routes, tests.
// Each feature module registers its DomainHandlers into DomainRegistry; the pipeline never branches on it.
export {};
