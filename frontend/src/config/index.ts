import appConfig from "./app.config";
import backendConfig from "./backend.config";

const config = {
  app: appConfig,
  backend: backendConfig,
};

export default config;
export { appConfig, backendConfig };
