// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: jagoo/v1/bridge.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { ErrorCode, errorCodeFromJSON, errorCodeToJSON, Priority, priorityFromJSON, priorityToJSON } from "./envelope";

export interface BridgeAnnounceReq {
  app_name: string;
  aspects: string[];
}

export interface BridgeAnnounceResp {
  destination_hash: string;
}

/**
 * RT-02: the bridge MUST reject PRIORITY_BULK with TRANSPORT_UNSUPPORTED. A LoRa link
 * cannot carry a forum feed, and attempting it starves the emergency channel.
 */
export interface SendReq {
  envelope: Uint8Array;
  /** "" => broadcast to the announced destination. */
  destination_hash: string;
  priority: Priority;
}

export interface SendResp {
  accepted: boolean;
  code: ErrorCode;
  fragments: number;
}

export interface ReceiveReq {
  destination_hash: string;
}

/**
 * RT-03: fragmentation and reassembly carry per-fragment integrity, so a partial
 * reassembly is discarded rather than delivered.
 */
export interface InboundEnvelope {
  envelope: Uint8Array;
  source_hash: string;
  rssi: number;
  snr: number;
  hops: number;
  received_at_ms: bigint;
}

export interface StatusReq {
}

export interface BridgeStatus {
  interfaces: RnsInterface[];
  paths: RnsPath[];
  queue_depth: number;
}

export interface RnsInterface {
  name: string;
  /** "TCPInterface" (demo, no hardware) | "RNodeInterface" (real LoRa) */
  kind: string;
  up: boolean;
  rssi: number;
  snr: number;
  tx_bytes: bigint;
  rx_bytes: bigint;
}

export interface RnsPath {
  destination_hash: string;
  hops: number;
  last_seen_ms: bigint;
}

function createBaseBridgeAnnounceReq(): BridgeAnnounceReq {
  return { app_name: "", aspects: [] };
}

export const BridgeAnnounceReq: MessageFns<BridgeAnnounceReq> = {
  encode(message: BridgeAnnounceReq, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.app_name !== "") {
      writer.uint32(10).string(message.app_name);
    }
    for (const v of message.aspects) {
      writer.uint32(18).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BridgeAnnounceReq {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBridgeAnnounceReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.app_name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.aspects.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BridgeAnnounceReq {
    return {
      app_name: isSet(object.appName)
        ? globalThis.String(object.appName)
        : isSet(object.app_name)
        ? globalThis.String(object.app_name)
        : "",
      aspects: globalThis.Array.isArray(object?.aspects) ? object.aspects.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: BridgeAnnounceReq): unknown {
    const obj: any = {};
    if (message.app_name !== "") {
      obj.appName = message.app_name;
    }
    if (message.aspects?.length) {
      obj.aspects = message.aspects;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BridgeAnnounceReq>, I>>(base?: I): BridgeAnnounceReq {
    return BridgeAnnounceReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BridgeAnnounceReq>, I>>(object: I): BridgeAnnounceReq {
    const message = createBaseBridgeAnnounceReq();
    message.app_name = object.app_name ?? "";
    message.aspects = object.aspects?.map((e) => e) || [];
    return message;
  },
};

function createBaseBridgeAnnounceResp(): BridgeAnnounceResp {
  return { destination_hash: "" };
}

export const BridgeAnnounceResp: MessageFns<BridgeAnnounceResp> = {
  encode(message: BridgeAnnounceResp, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.destination_hash !== "") {
      writer.uint32(10).string(message.destination_hash);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BridgeAnnounceResp {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBridgeAnnounceResp();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.destination_hash = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BridgeAnnounceResp {
    return {
      destination_hash: isSet(object.destinationHash)
        ? globalThis.String(object.destinationHash)
        : isSet(object.destination_hash)
        ? globalThis.String(object.destination_hash)
        : "",
    };
  },

  toJSON(message: BridgeAnnounceResp): unknown {
    const obj: any = {};
    if (message.destination_hash !== "") {
      obj.destinationHash = message.destination_hash;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BridgeAnnounceResp>, I>>(base?: I): BridgeAnnounceResp {
    return BridgeAnnounceResp.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BridgeAnnounceResp>, I>>(object: I): BridgeAnnounceResp {
    const message = createBaseBridgeAnnounceResp();
    message.destination_hash = object.destination_hash ?? "";
    return message;
  },
};

function createBaseSendReq(): SendReq {
  return { envelope: new Uint8Array(0), destination_hash: "", priority: 0 };
}

export const SendReq: MessageFns<SendReq> = {
  encode(message: SendReq, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.envelope.length !== 0) {
      writer.uint32(10).bytes(message.envelope);
    }
    if (message.destination_hash !== "") {
      writer.uint32(18).string(message.destination_hash);
    }
    if (message.priority !== 0) {
      writer.uint32(24).int32(message.priority);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SendReq {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSendReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.envelope = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.destination_hash = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.priority = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SendReq {
    return {
      envelope: isSet(object.envelope) ? bytesFromBase64(object.envelope) : new Uint8Array(0),
      destination_hash: isSet(object.destinationHash)
        ? globalThis.String(object.destinationHash)
        : isSet(object.destination_hash)
        ? globalThis.String(object.destination_hash)
        : "",
      priority: isSet(object.priority) ? priorityFromJSON(object.priority) : 0,
    };
  },

  toJSON(message: SendReq): unknown {
    const obj: any = {};
    if (message.envelope.length !== 0) {
      obj.envelope = base64FromBytes(message.envelope);
    }
    if (message.destination_hash !== "") {
      obj.destinationHash = message.destination_hash;
    }
    if (message.priority !== 0) {
      obj.priority = priorityToJSON(message.priority);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SendReq>, I>>(base?: I): SendReq {
    return SendReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SendReq>, I>>(object: I): SendReq {
    const message = createBaseSendReq();
    message.envelope = object.envelope ?? new Uint8Array(0);
    message.destination_hash = object.destination_hash ?? "";
    message.priority = object.priority ?? 0;
    return message;
  },
};

function createBaseSendResp(): SendResp {
  return { accepted: false, code: 0, fragments: 0 };
}

export const SendResp: MessageFns<SendResp> = {
  encode(message: SendResp, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.accepted !== false) {
      writer.uint32(8).bool(message.accepted);
    }
    if (message.code !== 0) {
      writer.uint32(16).int32(message.code);
    }
    if (message.fragments !== 0) {
      writer.uint32(24).uint32(message.fragments);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SendResp {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSendResp();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.accepted = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.fragments = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SendResp {
    return {
      accepted: isSet(object.accepted) ? globalThis.Boolean(object.accepted) : false,
      code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0,
      fragments: isSet(object.fragments) ? globalThis.Number(object.fragments) : 0,
    };
  },

  toJSON(message: SendResp): unknown {
    const obj: any = {};
    if (message.accepted !== false) {
      obj.accepted = message.accepted;
    }
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    if (message.fragments !== 0) {
      obj.fragments = Math.round(message.fragments);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SendResp>, I>>(base?: I): SendResp {
    return SendResp.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SendResp>, I>>(object: I): SendResp {
    const message = createBaseSendResp();
    message.accepted = object.accepted ?? false;
    message.code = object.code ?? 0;
    message.fragments = object.fragments ?? 0;
    return message;
  },
};

function createBaseReceiveReq(): ReceiveReq {
  return { destination_hash: "" };
}

export const ReceiveReq: MessageFns<ReceiveReq> = {
  encode(message: ReceiveReq, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.destination_hash !== "") {
      writer.uint32(10).string(message.destination_hash);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReceiveReq {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReceiveReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.destination_hash = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReceiveReq {
    return {
      destination_hash: isSet(object.destinationHash)
        ? globalThis.String(object.destinationHash)
        : isSet(object.destination_hash)
        ? globalThis.String(object.destination_hash)
        : "",
    };
  },

  toJSON(message: ReceiveReq): unknown {
    const obj: any = {};
    if (message.destination_hash !== "") {
      obj.destinationHash = message.destination_hash;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReceiveReq>, I>>(base?: I): ReceiveReq {
    return ReceiveReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReceiveReq>, I>>(object: I): ReceiveReq {
    const message = createBaseReceiveReq();
    message.destination_hash = object.destination_hash ?? "";
    return message;
  },
};

function createBaseInboundEnvelope(): InboundEnvelope {
  return { envelope: new Uint8Array(0), source_hash: "", rssi: 0, snr: 0, hops: 0, received_at_ms: 0n };
}

export const InboundEnvelope: MessageFns<InboundEnvelope> = {
  encode(message: InboundEnvelope, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.envelope.length !== 0) {
      writer.uint32(10).bytes(message.envelope);
    }
    if (message.source_hash !== "") {
      writer.uint32(18).string(message.source_hash);
    }
    if (message.rssi !== 0) {
      writer.uint32(24).int32(message.rssi);
    }
    if (message.snr !== 0) {
      writer.uint32(32).int32(message.snr);
    }
    if (message.hops !== 0) {
      writer.uint32(40).uint32(message.hops);
    }
    if (message.received_at_ms !== 0n) {
      if (BigInt.asIntN(64, message.received_at_ms) !== message.received_at_ms) {
        throw new globalThis.Error("value provided for field message.received_at_ms of type int64 too large");
      }
      writer.uint32(48).int64(message.received_at_ms);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): InboundEnvelope {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInboundEnvelope();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.envelope = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.source_hash = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.rssi = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.snr = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.hops = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.received_at_ms = reader.int64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): InboundEnvelope {
    return {
      envelope: isSet(object.envelope) ? bytesFromBase64(object.envelope) : new Uint8Array(0),
      source_hash: isSet(object.sourceHash)
        ? globalThis.String(object.sourceHash)
        : isSet(object.source_hash)
        ? globalThis.String(object.source_hash)
        : "",
      rssi: isSet(object.rssi) ? globalThis.Number(object.rssi) : 0,
      snr: isSet(object.snr) ? globalThis.Number(object.snr) : 0,
      hops: isSet(object.hops) ? globalThis.Number(object.hops) : 0,
      received_at_ms: isSet(object.receivedAtMs)
        ? BigInt(object.receivedAtMs)
        : isSet(object.received_at_ms)
        ? BigInt(object.received_at_ms)
        : 0n,
    };
  },

  toJSON(message: InboundEnvelope): unknown {
    const obj: any = {};
    if (message.envelope.length !== 0) {
      obj.envelope = base64FromBytes(message.envelope);
    }
    if (message.source_hash !== "") {
      obj.sourceHash = message.source_hash;
    }
    if (message.rssi !== 0) {
      obj.rssi = Math.round(message.rssi);
    }
    if (message.snr !== 0) {
      obj.snr = Math.round(message.snr);
    }
    if (message.hops !== 0) {
      obj.hops = Math.round(message.hops);
    }
    if (message.received_at_ms !== 0n) {
      obj.receivedAtMs = message.received_at_ms.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<InboundEnvelope>, I>>(base?: I): InboundEnvelope {
    return InboundEnvelope.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<InboundEnvelope>, I>>(object: I): InboundEnvelope {
    const message = createBaseInboundEnvelope();
    message.envelope = object.envelope ?? new Uint8Array(0);
    message.source_hash = object.source_hash ?? "";
    message.rssi = object.rssi ?? 0;
    message.snr = object.snr ?? 0;
    message.hops = object.hops ?? 0;
    message.received_at_ms = (object.received_at_ms !== undefined && object.received_at_ms !== null)
      ? BigInt(object.received_at_ms)
      : 0n;
    return message;
  },
};

function createBaseStatusReq(): StatusReq {
  return {};
}

export const StatusReq: MessageFns<StatusReq> = {
  encode(_: StatusReq, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StatusReq {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStatusReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): StatusReq {
    return {};
  },

  toJSON(_: StatusReq): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<StatusReq>, I>>(base?: I): StatusReq {
    return StatusReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StatusReq>, I>>(_: I): StatusReq {
    const message = createBaseStatusReq();
    return message;
  },
};

function createBaseBridgeStatus(): BridgeStatus {
  return { interfaces: [], paths: [], queue_depth: 0 };
}

export const BridgeStatus: MessageFns<BridgeStatus> = {
  encode(message: BridgeStatus, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.interfaces) {
      RnsInterface.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.paths) {
      RnsPath.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.queue_depth !== 0) {
      writer.uint32(24).uint32(message.queue_depth);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BridgeStatus {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBridgeStatus();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.interfaces.push(RnsInterface.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.paths.push(RnsPath.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.queue_depth = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BridgeStatus {
    return {
      interfaces: globalThis.Array.isArray(object?.interfaces)
        ? object.interfaces.map((e: any) => RnsInterface.fromJSON(e))
        : [],
      paths: globalThis.Array.isArray(object?.paths) ? object.paths.map((e: any) => RnsPath.fromJSON(e)) : [],
      queue_depth: isSet(object.queueDepth)
        ? globalThis.Number(object.queueDepth)
        : isSet(object.queue_depth)
        ? globalThis.Number(object.queue_depth)
        : 0,
    };
  },

  toJSON(message: BridgeStatus): unknown {
    const obj: any = {};
    if (message.interfaces?.length) {
      obj.interfaces = message.interfaces.map((e) => RnsInterface.toJSON(e));
    }
    if (message.paths?.length) {
      obj.paths = message.paths.map((e) => RnsPath.toJSON(e));
    }
    if (message.queue_depth !== 0) {
      obj.queueDepth = Math.round(message.queue_depth);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BridgeStatus>, I>>(base?: I): BridgeStatus {
    return BridgeStatus.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BridgeStatus>, I>>(object: I): BridgeStatus {
    const message = createBaseBridgeStatus();
    message.interfaces = object.interfaces?.map((e) => RnsInterface.fromPartial(e)) || [];
    message.paths = object.paths?.map((e) => RnsPath.fromPartial(e)) || [];
    message.queue_depth = object.queue_depth ?? 0;
    return message;
  },
};

function createBaseRnsInterface(): RnsInterface {
  return { name: "", kind: "", up: false, rssi: 0, snr: 0, tx_bytes: 0n, rx_bytes: 0n };
}

export const RnsInterface: MessageFns<RnsInterface> = {
  encode(message: RnsInterface, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.kind !== "") {
      writer.uint32(18).string(message.kind);
    }
    if (message.up !== false) {
      writer.uint32(24).bool(message.up);
    }
    if (message.rssi !== 0) {
      writer.uint32(32).int32(message.rssi);
    }
    if (message.snr !== 0) {
      writer.uint32(40).int32(message.snr);
    }
    if (message.tx_bytes !== 0n) {
      if (BigInt.asUintN(64, message.tx_bytes) !== message.tx_bytes) {
        throw new globalThis.Error("value provided for field message.tx_bytes of type uint64 too large");
      }
      writer.uint32(48).uint64(message.tx_bytes);
    }
    if (message.rx_bytes !== 0n) {
      if (BigInt.asUintN(64, message.rx_bytes) !== message.rx_bytes) {
        throw new globalThis.Error("value provided for field message.rx_bytes of type uint64 too large");
      }
      writer.uint32(56).uint64(message.rx_bytes);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RnsInterface {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRnsInterface();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.kind = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.up = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.rssi = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.snr = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.tx_bytes = reader.uint64() as bigint;
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.rx_bytes = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RnsInterface {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      kind: isSet(object.kind) ? globalThis.String(object.kind) : "",
      up: isSet(object.up) ? globalThis.Boolean(object.up) : false,
      rssi: isSet(object.rssi) ? globalThis.Number(object.rssi) : 0,
      snr: isSet(object.snr) ? globalThis.Number(object.snr) : 0,
      tx_bytes: isSet(object.txBytes) ? BigInt(object.txBytes) : isSet(object.tx_bytes) ? BigInt(object.tx_bytes) : 0n,
      rx_bytes: isSet(object.rxBytes) ? BigInt(object.rxBytes) : isSet(object.rx_bytes) ? BigInt(object.rx_bytes) : 0n,
    };
  },

  toJSON(message: RnsInterface): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.kind !== "") {
      obj.kind = message.kind;
    }
    if (message.up !== false) {
      obj.up = message.up;
    }
    if (message.rssi !== 0) {
      obj.rssi = Math.round(message.rssi);
    }
    if (message.snr !== 0) {
      obj.snr = Math.round(message.snr);
    }
    if (message.tx_bytes !== 0n) {
      obj.txBytes = message.tx_bytes.toString();
    }
    if (message.rx_bytes !== 0n) {
      obj.rxBytes = message.rx_bytes.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RnsInterface>, I>>(base?: I): RnsInterface {
    return RnsInterface.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RnsInterface>, I>>(object: I): RnsInterface {
    const message = createBaseRnsInterface();
    message.name = object.name ?? "";
    message.kind = object.kind ?? "";
    message.up = object.up ?? false;
    message.rssi = object.rssi ?? 0;
    message.snr = object.snr ?? 0;
    message.tx_bytes = (object.tx_bytes !== undefined && object.tx_bytes !== null) ? BigInt(object.tx_bytes) : 0n;
    message.rx_bytes = (object.rx_bytes !== undefined && object.rx_bytes !== null) ? BigInt(object.rx_bytes) : 0n;
    return message;
  },
};

function createBaseRnsPath(): RnsPath {
  return { destination_hash: "", hops: 0, last_seen_ms: 0n };
}

export const RnsPath: MessageFns<RnsPath> = {
  encode(message: RnsPath, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.destination_hash !== "") {
      writer.uint32(10).string(message.destination_hash);
    }
    if (message.hops !== 0) {
      writer.uint32(16).uint32(message.hops);
    }
    if (message.last_seen_ms !== 0n) {
      if (BigInt.asIntN(64, message.last_seen_ms) !== message.last_seen_ms) {
        throw new globalThis.Error("value provided for field message.last_seen_ms of type int64 too large");
      }
      writer.uint32(24).int64(message.last_seen_ms);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RnsPath {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRnsPath();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.destination_hash = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.hops = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.last_seen_ms = reader.int64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RnsPath {
    return {
      destination_hash: isSet(object.destinationHash)
        ? globalThis.String(object.destinationHash)
        : isSet(object.destination_hash)
        ? globalThis.String(object.destination_hash)
        : "",
      hops: isSet(object.hops) ? globalThis.Number(object.hops) : 0,
      last_seen_ms: isSet(object.lastSeenMs)
        ? BigInt(object.lastSeenMs)
        : isSet(object.last_seen_ms)
        ? BigInt(object.last_seen_ms)
        : 0n,
    };
  },

  toJSON(message: RnsPath): unknown {
    const obj: any = {};
    if (message.destination_hash !== "") {
      obj.destinationHash = message.destination_hash;
    }
    if (message.hops !== 0) {
      obj.hops = Math.round(message.hops);
    }
    if (message.last_seen_ms !== 0n) {
      obj.lastSeenMs = message.last_seen_ms.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RnsPath>, I>>(base?: I): RnsPath {
    return RnsPath.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RnsPath>, I>>(object: I): RnsPath {
    const message = createBaseRnsPath();
    message.destination_hash = object.destination_hash ?? "";
    message.hops = object.hops ?? 0;
    message.last_seen_ms = (object.last_seen_ms !== undefined && object.last_seen_ms !== null)
      ? BigInt(object.last_seen_ms)
      : 0n;
    return message;
  },
};

export type ReticulumBridgeDefinition = typeof ReticulumBridgeDefinition;
export const ReticulumBridgeDefinition = {
  name: "ReticulumBridge",
  fullName: "jagoo.v1.ReticulumBridge",
  methods: {
    /** Publish a destination. */
    announce: {
      name: "Announce",
      requestType: BridgeAnnounceReq as typeof BridgeAnnounceReq,
      requestStream: false,
      responseType: BridgeAnnounceResp as typeof BridgeAnnounceResp,
      responseStream: false,
      options: {},
    },
    /** Class 0-2 only. */
    send: {
      name: "Send",
      requestType: SendReq as typeof SendReq,
      requestStream: false,
      responseType: SendResp as typeof SendResp,
      responseStream: false,
      options: {},
    },
    receive: {
      name: "Receive",
      requestType: ReceiveReq as typeof ReceiveReq,
      requestStream: false,
      responseType: InboundEnvelope as typeof InboundEnvelope,
      responseStream: true,
      options: {},
    },
    status: {
      name: "Status",
      requestType: StatusReq as typeof StatusReq,
      requestStream: false,
      responseType: BridgeStatus as typeof BridgeStatus,
      responseStream: false,
      options: {},
    },
  },
} as const;

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

type DeepPartial<T> = T extends bigint ? string | number | bigint
  : T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
