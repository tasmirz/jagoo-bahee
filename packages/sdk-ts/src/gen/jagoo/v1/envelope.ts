// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: jagoo/v1/envelope.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

/**
 * The plane is a signed field, which is what makes cross-plane replay structurally
 * impossible rather than merely checked (SEP-02). Combined with the registry mapping
 * each domain to exactly one plane, a FORUM signature can never be lifted into a
 * SIGNAL envelope.
 */
export enum Plane {
  PLANE_UNSPECIFIED = 0,
  /** PLANE_FORUM - anonymous / pseudonymous */
  PLANE_FORUM = 1,
  /** PLANE_SIGNAL - identified */
  PLANE_SIGNAL = 2,
}

export function planeFromJSON(object: any): Plane {
  switch (object) {
    case 0:
    case "PLANE_UNSPECIFIED":
      return Plane.PLANE_UNSPECIFIED;
    case 1:
    case "PLANE_FORUM":
      return Plane.PLANE_FORUM;
    case 2:
    case "PLANE_SIGNAL":
      return Plane.PLANE_SIGNAL;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum Plane");
  }
}

export function planeToJSON(object: Plane): string {
  switch (object) {
    case Plane.PLANE_UNSPECIFIED:
      return "PLANE_UNSPECIFIED";
    case Plane.PLANE_FORUM:
      return "PLANE_FORUM";
    case Plane.PLANE_SIGNAL:
      return "PLANE_SIGNAL";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum Plane");
  }
}

/**
 * KY-05: BROADCAST, DIRECT and CHECKIN priority envelopes MUST use a 64-byte
 * signature algorithm. One ML-DSA signature is ~12 LoRa frames before any content.
 */
export enum KeyAlg {
  KEY_ALG_UNSPECIFIED = 0,
  /** KEY_ALG_ED25519 - 64 B signature — default for all per-message signing */
  KEY_ALG_ED25519 = 1,
  /** KEY_ALG_ML_DSA_44 - 2420 B — certificates only */
  KEY_ALG_ML_DSA_44 = 2,
  /** KEY_ALG_FALCON_512 - reserved */
  KEY_ALG_FALCON_512 = 3,
}

export function keyAlgFromJSON(object: any): KeyAlg {
  switch (object) {
    case 0:
    case "KEY_ALG_UNSPECIFIED":
      return KeyAlg.KEY_ALG_UNSPECIFIED;
    case 1:
    case "KEY_ALG_ED25519":
      return KeyAlg.KEY_ALG_ED25519;
    case 2:
    case "KEY_ALG_ML_DSA_44":
      return KeyAlg.KEY_ALG_ML_DSA_44;
    case 3:
    case "KEY_ALG_FALCON_512":
      return KeyAlg.KEY_ALG_FALCON_512;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum KeyAlg");
  }
}

export function keyAlgToJSON(object: KeyAlg): string {
  switch (object) {
    case KeyAlg.KEY_ALG_UNSPECIFIED:
      return "KEY_ALG_UNSPECIFIED";
    case KeyAlg.KEY_ALG_ED25519:
      return "KEY_ALG_ED25519";
    case KeyAlg.KEY_ALG_ML_DSA_44:
      return "KEY_ALG_ML_DSA_44";
    case KeyAlg.KEY_ALG_FALCON_512:
      return "KEY_ALG_FALCON_512";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum KeyAlg");
  }
}

/**
 * PC-03: outbound queues are ordered by priority class first, then FIFO.
 * A queued emergency broadcast overtakes 500 queued votes.
 */
export enum Priority {
  PRIORITY_UNSPECIFIED = 0,
  /** PRIORITY_BROADCAST - class 0 — emergency, key revocation.  <= 512 B */
  PRIORITY_BROADCAST = 1,
  /** PRIORITY_DIRECT - class 1 — messages.                   <= 1 KB */
  PRIORITY_DIRECT = 2,
  /** PRIORITY_CHECKIN - class 2 — safe-status, resources.     <= 512 B */
  PRIORITY_CHECKIN = 3,
  /** PRIORITY_BULK - class 3 — posts, comments, votes.     unbounded, rejected by Reticulum */
  PRIORITY_BULK = 4,
}

export function priorityFromJSON(object: any): Priority {
  switch (object) {
    case 0:
    case "PRIORITY_UNSPECIFIED":
      return Priority.PRIORITY_UNSPECIFIED;
    case 1:
    case "PRIORITY_BROADCAST":
      return Priority.PRIORITY_BROADCAST;
    case 2:
    case "PRIORITY_DIRECT":
      return Priority.PRIORITY_DIRECT;
    case 3:
    case "PRIORITY_CHECKIN":
      return Priority.PRIORITY_CHECKIN;
    case 4:
    case "PRIORITY_BULK":
      return Priority.PRIORITY_BULK;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum Priority");
  }
}

export function priorityToJSON(object: Priority): string {
  switch (object) {
    case Priority.PRIORITY_UNSPECIFIED:
      return "PRIORITY_UNSPECIFIED";
    case Priority.PRIORITY_BROADCAST:
      return "PRIORITY_BROADCAST";
    case Priority.PRIORITY_DIRECT:
      return "PRIORITY_DIRECT";
    case Priority.PRIORITY_CHECKIN:
      return "PRIORITY_CHECKIN";
    case Priority.PRIORITY_BULK:
      return "PRIORITY_BULK";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum Priority");
  }
}

/**
 * ER-02: error responses MUST NOT reveal whether a key exists, what a user's other
 * content is, or any cross-plane information.
 */
export enum ErrorCode {
  ERROR_CODE_UNSPECIFIED = 0,
  ERROR_CODE_UNKNOWN_VERSION = 1,
  ERROR_CODE_UNKNOWN_DOMAIN = 2,
  ERROR_CODE_PLANE_MISMATCH = 3,
  ERROR_CODE_MALFORMED = 4,
  ERROR_CODE_ALG_NOT_PERMITTED = 5,
  ERROR_CODE_PRIORITY_MISMATCH = 6,
  ERROR_CODE_CLOCK_SKEW = 7,
  ERROR_CODE_BAD_SIGNATURE = 8,
  ERROR_CODE_NO_CERTIFICATE = 9,
  ERROR_CODE_KEY_REVOKED = 10,
  ERROR_CODE_DUPLICATE = 11,
  ERROR_CODE_REPLAY = 12,
  ERROR_CODE_INSUFFICIENT_CREDITS = 13,
  ERROR_CODE_NULLIFIER_SPENT = 14,
  ERROR_CODE_CREDENTIAL_INVALID = 15,
  ERROR_CODE_FORBIDDEN = 16,
  ERROR_CODE_BODY_INVALID = 17,
  ERROR_CODE_TOO_LARGE = 18,
  ERROR_CODE_RATE_LIMITED = 19,
  ERROR_CODE_TRANSPORT_UNSUPPORTED = 20,
}

export function errorCodeFromJSON(object: any): ErrorCode {
  switch (object) {
    case 0:
    case "ERROR_CODE_UNSPECIFIED":
      return ErrorCode.ERROR_CODE_UNSPECIFIED;
    case 1:
    case "ERROR_CODE_UNKNOWN_VERSION":
      return ErrorCode.ERROR_CODE_UNKNOWN_VERSION;
    case 2:
    case "ERROR_CODE_UNKNOWN_DOMAIN":
      return ErrorCode.ERROR_CODE_UNKNOWN_DOMAIN;
    case 3:
    case "ERROR_CODE_PLANE_MISMATCH":
      return ErrorCode.ERROR_CODE_PLANE_MISMATCH;
    case 4:
    case "ERROR_CODE_MALFORMED":
      return ErrorCode.ERROR_CODE_MALFORMED;
    case 5:
    case "ERROR_CODE_ALG_NOT_PERMITTED":
      return ErrorCode.ERROR_CODE_ALG_NOT_PERMITTED;
    case 6:
    case "ERROR_CODE_PRIORITY_MISMATCH":
      return ErrorCode.ERROR_CODE_PRIORITY_MISMATCH;
    case 7:
    case "ERROR_CODE_CLOCK_SKEW":
      return ErrorCode.ERROR_CODE_CLOCK_SKEW;
    case 8:
    case "ERROR_CODE_BAD_SIGNATURE":
      return ErrorCode.ERROR_CODE_BAD_SIGNATURE;
    case 9:
    case "ERROR_CODE_NO_CERTIFICATE":
      return ErrorCode.ERROR_CODE_NO_CERTIFICATE;
    case 10:
    case "ERROR_CODE_KEY_REVOKED":
      return ErrorCode.ERROR_CODE_KEY_REVOKED;
    case 11:
    case "ERROR_CODE_DUPLICATE":
      return ErrorCode.ERROR_CODE_DUPLICATE;
    case 12:
    case "ERROR_CODE_REPLAY":
      return ErrorCode.ERROR_CODE_REPLAY;
    case 13:
    case "ERROR_CODE_INSUFFICIENT_CREDITS":
      return ErrorCode.ERROR_CODE_INSUFFICIENT_CREDITS;
    case 14:
    case "ERROR_CODE_NULLIFIER_SPENT":
      return ErrorCode.ERROR_CODE_NULLIFIER_SPENT;
    case 15:
    case "ERROR_CODE_CREDENTIAL_INVALID":
      return ErrorCode.ERROR_CODE_CREDENTIAL_INVALID;
    case 16:
    case "ERROR_CODE_FORBIDDEN":
      return ErrorCode.ERROR_CODE_FORBIDDEN;
    case 17:
    case "ERROR_CODE_BODY_INVALID":
      return ErrorCode.ERROR_CODE_BODY_INVALID;
    case 18:
    case "ERROR_CODE_TOO_LARGE":
      return ErrorCode.ERROR_CODE_TOO_LARGE;
    case 19:
    case "ERROR_CODE_RATE_LIMITED":
      return ErrorCode.ERROR_CODE_RATE_LIMITED;
    case 20:
    case "ERROR_CODE_TRANSPORT_UNSUPPORTED":
      return ErrorCode.ERROR_CODE_TRANSPORT_UNSUPPORTED;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum ErrorCode");
  }
}

export function errorCodeToJSON(object: ErrorCode): string {
  switch (object) {
    case ErrorCode.ERROR_CODE_UNSPECIFIED:
      return "ERROR_CODE_UNSPECIFIED";
    case ErrorCode.ERROR_CODE_UNKNOWN_VERSION:
      return "ERROR_CODE_UNKNOWN_VERSION";
    case ErrorCode.ERROR_CODE_UNKNOWN_DOMAIN:
      return "ERROR_CODE_UNKNOWN_DOMAIN";
    case ErrorCode.ERROR_CODE_PLANE_MISMATCH:
      return "ERROR_CODE_PLANE_MISMATCH";
    case ErrorCode.ERROR_CODE_MALFORMED:
      return "ERROR_CODE_MALFORMED";
    case ErrorCode.ERROR_CODE_ALG_NOT_PERMITTED:
      return "ERROR_CODE_ALG_NOT_PERMITTED";
    case ErrorCode.ERROR_CODE_PRIORITY_MISMATCH:
      return "ERROR_CODE_PRIORITY_MISMATCH";
    case ErrorCode.ERROR_CODE_CLOCK_SKEW:
      return "ERROR_CODE_CLOCK_SKEW";
    case ErrorCode.ERROR_CODE_BAD_SIGNATURE:
      return "ERROR_CODE_BAD_SIGNATURE";
    case ErrorCode.ERROR_CODE_NO_CERTIFICATE:
      return "ERROR_CODE_NO_CERTIFICATE";
    case ErrorCode.ERROR_CODE_KEY_REVOKED:
      return "ERROR_CODE_KEY_REVOKED";
    case ErrorCode.ERROR_CODE_DUPLICATE:
      return "ERROR_CODE_DUPLICATE";
    case ErrorCode.ERROR_CODE_REPLAY:
      return "ERROR_CODE_REPLAY";
    case ErrorCode.ERROR_CODE_INSUFFICIENT_CREDITS:
      return "ERROR_CODE_INSUFFICIENT_CREDITS";
    case ErrorCode.ERROR_CODE_NULLIFIER_SPENT:
      return "ERROR_CODE_NULLIFIER_SPENT";
    case ErrorCode.ERROR_CODE_CREDENTIAL_INVALID:
      return "ERROR_CODE_CREDENTIAL_INVALID";
    case ErrorCode.ERROR_CODE_FORBIDDEN:
      return "ERROR_CODE_FORBIDDEN";
    case ErrorCode.ERROR_CODE_BODY_INVALID:
      return "ERROR_CODE_BODY_INVALID";
    case ErrorCode.ERROR_CODE_TOO_LARGE:
      return "ERROR_CODE_TOO_LARGE";
    case ErrorCode.ERROR_CODE_RATE_LIMITED:
      return "ERROR_CODE_RATE_LIMITED";
    case ErrorCode.ERROR_CODE_TRANSPORT_UNSUPPORTED:
      return "ERROR_CODE_TRANSPORT_UNSUPPORTED";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum ErrorCode");
  }
}

export enum RevocationKind {
  REVOCATION_KIND_UNSPECIFIED = 0,
  /** REVOCATION_KIND_ROTATE - Planned; replacement inherits standing. */
  REVOCATION_KIND_ROTATE = 1,
  /** REVOCATION_KIND_COMPROMISE - Content signed after effective_from is untrusted. */
  REVOCATION_KIND_COMPROMISE = 2,
  /** REVOCATION_KIND_DURESS - Pre-signed, third-party publishable; triggers wipe (KY-02). */
  REVOCATION_KIND_DURESS = 3,
  /** REVOCATION_KIND_RETIRE - Identity or channel stops publishing. */
  REVOCATION_KIND_RETIRE = 4,
}

export function revocationKindFromJSON(object: any): RevocationKind {
  switch (object) {
    case 0:
    case "REVOCATION_KIND_UNSPECIFIED":
      return RevocationKind.REVOCATION_KIND_UNSPECIFIED;
    case 1:
    case "REVOCATION_KIND_ROTATE":
      return RevocationKind.REVOCATION_KIND_ROTATE;
    case 2:
    case "REVOCATION_KIND_COMPROMISE":
      return RevocationKind.REVOCATION_KIND_COMPROMISE;
    case 3:
    case "REVOCATION_KIND_DURESS":
      return RevocationKind.REVOCATION_KIND_DURESS;
    case 4:
    case "REVOCATION_KIND_RETIRE":
      return RevocationKind.REVOCATION_KIND_RETIRE;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum RevocationKind");
  }
}

export function revocationKindToJSON(object: RevocationKind): string {
  switch (object) {
    case RevocationKind.REVOCATION_KIND_UNSPECIFIED:
      return "REVOCATION_KIND_UNSPECIFIED";
    case RevocationKind.REVOCATION_KIND_ROTATE:
      return "REVOCATION_KIND_ROTATE";
    case RevocationKind.REVOCATION_KIND_COMPROMISE:
      return "REVOCATION_KIND_COMPROMISE";
    case RevocationKind.REVOCATION_KIND_DURESS:
      return "REVOCATION_KIND_DURESS";
    case RevocationKind.REVOCATION_KIND_RETIRE:
      return "REVOCATION_KIND_RETIRE";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum RevocationKind");
  }
}

/**
 * ─────────────────────────────────────────────────────────────────────────────
 * The universal signed unit. Every mutation in the system is one of these.
 * ─────────────────────────────────────────────────────────────────────────────
 */
export interface Envelope {
  /** MUST be 1. Unknown -> hard reject (EN-02). */
  version: number;
  /** Inside the signature (SEP-02). */
  plane: Plane;
  /** e.g. "jb:post:create:v1" — must exist in the registry. */
  domain: string;
  /** Raw public key. Identity IS this value (VIS-02). */
  author_key: Uint8Array;
  key_alg: KeyAlg;
  /** content_id of parent, "" if none. */
  parent: string;
  /** community_id (FORUM) | channel_id (SIGNAL) | "". */
  scope: string;
  /** Within [now - max_age, now + max_skew]. */
  created_at_ms: bigint;
  /** 16 bytes. Required on non-idempotent domains. */
  nonce: Uint8Array;
  /** MUST equal the registry's declared class. */
  priority: Priority;
  /** Serialized typed body. */
  body: Uint8Array;
  /** MAY be empty on MESH / RETICULUM transports. */
  anti_abuse?:
    | AntiAbuse
    | undefined;
  /** Over canonical bytes of fields 1..12. */
  signature: Uint8Array;
}

/**
 * VIS-07 / THR-08: rate limiting must work against a fully anonymous actor,
 * because requiring identity for spam control hands the adversary a censorship lever.
 */
export interface AntiAbuse {
  /** Unblinded membership token (FORUM). */
  credential: Uint8Array;
  /** H(domain || epoch || secret) (FORUM). */
  nullifier: Uint8Array;
  epoch: number;
  /** Argon2id proof, when demanded. */
  pow: Uint8Array;
}

/**
 * TL-03: carries an inclusion proof small enough for a client to store offline as
 * durable proof of publication. ER-01: a DUPLICATE returns the ORIGINAL receipt.
 */
export interface Receipt {
  content_id: string;
  /** Server key fingerprint, NOT a URL (FD-02). */
  server_id: string;
  server_key: Uint8Array;
  accepted_at_ms: bigint;
  log_index: bigint;
  sth?: SignedTreeHead | undefined;
  inclusion_proof: Uint8Array[];
  server_signature: Uint8Array;
}

/**
 * TL-02: published at least every 60 seconds and on demand.
 * TL-05: gossiped between peers, so a node that forks its log is caught by its peers.
 */
export interface SignedTreeHead {
  server_key: Uint8Array;
  tree_size: bigint;
  root_hash: Uint8Array;
  timestamp_ms: bigint;
  signature: Uint8Array;
}

/**
 * ─────────────────────────────────────────────────────────────────────────────
 * Error contract (Plans/02 §6)
 * ─────────────────────────────────────────────────────────────────────────────
 */
export interface EnvelopeError {
  code: ErrorCode;
  detail: string;
  /** Offending field path, when applicable. */
  field: string;
  retry_after_ms: bigint;
  /** Present on INSUFFICIENT_CREDITS / RATE_LIMITED. */
  challenge?: PowChallenge | undefined;
}

/**
 * AB-04: memory-hard and bound to the requesting key. Challenge issuance MUST be
 * stateless (HMAC-derived) so issuing challenges cannot exhaust server memory —
 * v1 stored every issued challenge in Redis with no issuance limit.
 */
export interface PowChallenge {
  /** HMAC(server_secret, key || window) — stateless, not stored. */
  challenge: string;
  /** "argon2id" */
  algorithm: string;
  /** Scaled by live system pressure. */
  memory_kib: number;
  iterations: number;
  parallelism: number;
  /** Author key — work is non-transferable. */
  bound_to: Uint8Array;
  expires_at_ms: bigint;
}

export interface PowSolution {
  challenge: string;
  solution: Uint8Array;
  bound_to: Uint8Array;
}

/**
 * The PQ budget is spent by actual threat: an ML-DSA identity certificate signed once
 * and cached, so peers verify a 64-byte Ed25519 signature against an Ed25519 key that
 * is itself post-quantum attested.
 */
export interface KeyCertificate {
  plane: Plane;
  /** Ed25519, 32 B. */
  device_key: Uint8Array;
  /** ML-DSA-44, 1312 B. */
  pq_key: Uint8Array;
  /** ML-DSA sig over device_key || valid_from || valid_until. */
  pq_attestation: Uint8Array;
  valid_from: bigint;
  valid_until: bigint;
  /** Ed25519. */
  self_signature: Uint8Array;
}

/**
 * KY-01: revocation MUST NOT invalidate content signed before effective_from_ms.
 * Retroactive erasure of a person's history is itself a censorship vector.
 */
export interface KeyRevocation {
  plane: Plane;
  revoked_key: Uint8Array;
  kind: RevocationKind;
  effective_from_ms: bigint;
  replacement_key: Uint8Array;
}

function createBaseEnvelope(): Envelope {
  return {
    version: 0,
    plane: 0,
    domain: "",
    author_key: new Uint8Array(0),
    key_alg: 0,
    parent: "",
    scope: "",
    created_at_ms: 0n,
    nonce: new Uint8Array(0),
    priority: 0,
    body: new Uint8Array(0),
    anti_abuse: undefined,
    signature: new Uint8Array(0),
  };
}

export const Envelope: MessageFns<Envelope> = {
  encode(message: Envelope, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.version !== 0) {
      writer.uint32(8).uint32(message.version);
    }
    if (message.plane !== 0) {
      writer.uint32(16).int32(message.plane);
    }
    if (message.domain !== "") {
      writer.uint32(26).string(message.domain);
    }
    if (message.author_key.length !== 0) {
      writer.uint32(34).bytes(message.author_key);
    }
    if (message.key_alg !== 0) {
      writer.uint32(40).int32(message.key_alg);
    }
    if (message.parent !== "") {
      writer.uint32(50).string(message.parent);
    }
    if (message.scope !== "") {
      writer.uint32(58).string(message.scope);
    }
    if (message.created_at_ms !== 0n) {
      if (BigInt.asIntN(64, message.created_at_ms) !== message.created_at_ms) {
        throw new globalThis.Error("value provided for field message.created_at_ms of type int64 too large");
      }
      writer.uint32(64).int64(message.created_at_ms);
    }
    if (message.nonce.length !== 0) {
      writer.uint32(74).bytes(message.nonce);
    }
    if (message.priority !== 0) {
      writer.uint32(80).int32(message.priority);
    }
    if (message.body.length !== 0) {
      writer.uint32(90).bytes(message.body);
    }
    if (message.anti_abuse !== undefined) {
      AntiAbuse.encode(message.anti_abuse, writer.uint32(98).fork()).join();
    }
    if (message.signature.length !== 0) {
      writer.uint32(106).bytes(message.signature);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Envelope {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEnvelope();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.version = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.plane = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.domain = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.author_key = reader.bytes();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.key_alg = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.parent = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.scope = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.created_at_ms = reader.int64() as bigint;
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.nonce = reader.bytes();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.priority = reader.int32() as any;
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.body = reader.bytes();
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.anti_abuse = AntiAbuse.decode(reader, reader.uint32());
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.signature = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Envelope {
    return {
      version: isSet(object.version) ? globalThis.Number(object.version) : 0,
      plane: isSet(object.plane) ? planeFromJSON(object.plane) : 0,
      domain: isSet(object.domain) ? globalThis.String(object.domain) : "",
      author_key: isSet(object.authorKey)
        ? bytesFromBase64(object.authorKey)
        : isSet(object.author_key)
        ? bytesFromBase64(object.author_key)
        : new Uint8Array(0),
      key_alg: isSet(object.keyAlg)
        ? keyAlgFromJSON(object.keyAlg)
        : isSet(object.key_alg)
        ? keyAlgFromJSON(object.key_alg)
        : 0,
      parent: isSet(object.parent) ? globalThis.String(object.parent) : "",
      scope: isSet(object.scope) ? globalThis.String(object.scope) : "",
      created_at_ms: isSet(object.createdAtMs)
        ? BigInt(object.createdAtMs)
        : isSet(object.created_at_ms)
        ? BigInt(object.created_at_ms)
        : 0n,
      nonce: isSet(object.nonce) ? bytesFromBase64(object.nonce) : new Uint8Array(0),
      priority: isSet(object.priority) ? priorityFromJSON(object.priority) : 0,
      body: isSet(object.body) ? bytesFromBase64(object.body) : new Uint8Array(0),
      anti_abuse: isSet(object.antiAbuse)
        ? AntiAbuse.fromJSON(object.antiAbuse)
        : isSet(object.anti_abuse)
        ? AntiAbuse.fromJSON(object.anti_abuse)
        : undefined,
      signature: isSet(object.signature) ? bytesFromBase64(object.signature) : new Uint8Array(0),
    };
  },

  toJSON(message: Envelope): unknown {
    const obj: any = {};
    if (message.version !== 0) {
      obj.version = Math.round(message.version);
    }
    if (message.plane !== 0) {
      obj.plane = planeToJSON(message.plane);
    }
    if (message.domain !== "") {
      obj.domain = message.domain;
    }
    if (message.author_key.length !== 0) {
      obj.authorKey = base64FromBytes(message.author_key);
    }
    if (message.key_alg !== 0) {
      obj.keyAlg = keyAlgToJSON(message.key_alg);
    }
    if (message.parent !== "") {
      obj.parent = message.parent;
    }
    if (message.scope !== "") {
      obj.scope = message.scope;
    }
    if (message.created_at_ms !== 0n) {
      obj.createdAtMs = message.created_at_ms.toString();
    }
    if (message.nonce.length !== 0) {
      obj.nonce = base64FromBytes(message.nonce);
    }
    if (message.priority !== 0) {
      obj.priority = priorityToJSON(message.priority);
    }
    if (message.body.length !== 0) {
      obj.body = base64FromBytes(message.body);
    }
    if (message.anti_abuse !== undefined) {
      obj.antiAbuse = AntiAbuse.toJSON(message.anti_abuse);
    }
    if (message.signature.length !== 0) {
      obj.signature = base64FromBytes(message.signature);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Envelope>, I>>(base?: I): Envelope {
    return Envelope.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Envelope>, I>>(object: I): Envelope {
    const message = createBaseEnvelope();
    message.version = object.version ?? 0;
    message.plane = object.plane ?? 0;
    message.domain = object.domain ?? "";
    message.author_key = object.author_key ?? new Uint8Array(0);
    message.key_alg = object.key_alg ?? 0;
    message.parent = object.parent ?? "";
    message.scope = object.scope ?? "";
    message.created_at_ms = (object.created_at_ms !== undefined && object.created_at_ms !== null)
      ? BigInt(object.created_at_ms)
      : 0n;
    message.nonce = object.nonce ?? new Uint8Array(0);
    message.priority = object.priority ?? 0;
    message.body = object.body ?? new Uint8Array(0);
    message.anti_abuse = (object.anti_abuse !== undefined && object.anti_abuse !== null)
      ? AntiAbuse.fromPartial(object.anti_abuse)
      : undefined;
    message.signature = object.signature ?? new Uint8Array(0);
    return message;
  },
};

function createBaseAntiAbuse(): AntiAbuse {
  return { credential: new Uint8Array(0), nullifier: new Uint8Array(0), epoch: 0, pow: new Uint8Array(0) };
}

export const AntiAbuse: MessageFns<AntiAbuse> = {
  encode(message: AntiAbuse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.credential.length !== 0) {
      writer.uint32(10).bytes(message.credential);
    }
    if (message.nullifier.length !== 0) {
      writer.uint32(18).bytes(message.nullifier);
    }
    if (message.epoch !== 0) {
      writer.uint32(24).uint32(message.epoch);
    }
    if (message.pow.length !== 0) {
      writer.uint32(34).bytes(message.pow);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AntiAbuse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAntiAbuse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.credential = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.nullifier = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.epoch = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.pow = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AntiAbuse {
    return {
      credential: isSet(object.credential) ? bytesFromBase64(object.credential) : new Uint8Array(0),
      nullifier: isSet(object.nullifier) ? bytesFromBase64(object.nullifier) : new Uint8Array(0),
      epoch: isSet(object.epoch) ? globalThis.Number(object.epoch) : 0,
      pow: isSet(object.pow) ? bytesFromBase64(object.pow) : new Uint8Array(0),
    };
  },

  toJSON(message: AntiAbuse): unknown {
    const obj: any = {};
    if (message.credential.length !== 0) {
      obj.credential = base64FromBytes(message.credential);
    }
    if (message.nullifier.length !== 0) {
      obj.nullifier = base64FromBytes(message.nullifier);
    }
    if (message.epoch !== 0) {
      obj.epoch = Math.round(message.epoch);
    }
    if (message.pow.length !== 0) {
      obj.pow = base64FromBytes(message.pow);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AntiAbuse>, I>>(base?: I): AntiAbuse {
    return AntiAbuse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AntiAbuse>, I>>(object: I): AntiAbuse {
    const message = createBaseAntiAbuse();
    message.credential = object.credential ?? new Uint8Array(0);
    message.nullifier = object.nullifier ?? new Uint8Array(0);
    message.epoch = object.epoch ?? 0;
    message.pow = object.pow ?? new Uint8Array(0);
    return message;
  },
};

function createBaseReceipt(): Receipt {
  return {
    content_id: "",
    server_id: "",
    server_key: new Uint8Array(0),
    accepted_at_ms: 0n,
    log_index: 0n,
    sth: undefined,
    inclusion_proof: [],
    server_signature: new Uint8Array(0),
  };
}

export const Receipt: MessageFns<Receipt> = {
  encode(message: Receipt, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.content_id !== "") {
      writer.uint32(10).string(message.content_id);
    }
    if (message.server_id !== "") {
      writer.uint32(18).string(message.server_id);
    }
    if (message.server_key.length !== 0) {
      writer.uint32(26).bytes(message.server_key);
    }
    if (message.accepted_at_ms !== 0n) {
      if (BigInt.asIntN(64, message.accepted_at_ms) !== message.accepted_at_ms) {
        throw new globalThis.Error("value provided for field message.accepted_at_ms of type int64 too large");
      }
      writer.uint32(32).int64(message.accepted_at_ms);
    }
    if (message.log_index !== 0n) {
      if (BigInt.asUintN(64, message.log_index) !== message.log_index) {
        throw new globalThis.Error("value provided for field message.log_index of type uint64 too large");
      }
      writer.uint32(40).uint64(message.log_index);
    }
    if (message.sth !== undefined) {
      SignedTreeHead.encode(message.sth, writer.uint32(50).fork()).join();
    }
    for (const v of message.inclusion_proof) {
      writer.uint32(58).bytes(v!);
    }
    if (message.server_signature.length !== 0) {
      writer.uint32(66).bytes(message.server_signature);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Receipt {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReceipt();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.content_id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.server_id = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.server_key = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.accepted_at_ms = reader.int64() as bigint;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.log_index = reader.uint64() as bigint;
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.sth = SignedTreeHead.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.inclusion_proof.push(reader.bytes());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.server_signature = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Receipt {
    return {
      content_id: isSet(object.contentId)
        ? globalThis.String(object.contentId)
        : isSet(object.content_id)
        ? globalThis.String(object.content_id)
        : "",
      server_id: isSet(object.serverId)
        ? globalThis.String(object.serverId)
        : isSet(object.server_id)
        ? globalThis.String(object.server_id)
        : "",
      server_key: isSet(object.serverKey)
        ? bytesFromBase64(object.serverKey)
        : isSet(object.server_key)
        ? bytesFromBase64(object.server_key)
        : new Uint8Array(0),
      accepted_at_ms: isSet(object.acceptedAtMs)
        ? BigInt(object.acceptedAtMs)
        : isSet(object.accepted_at_ms)
        ? BigInt(object.accepted_at_ms)
        : 0n,
      log_index: isSet(object.logIndex)
        ? BigInt(object.logIndex)
        : isSet(object.log_index)
        ? BigInt(object.log_index)
        : 0n,
      sth: isSet(object.sth) ? SignedTreeHead.fromJSON(object.sth) : undefined,
      inclusion_proof: globalThis.Array.isArray(object?.inclusionProof)
        ? object.inclusionProof.map((e: any) => bytesFromBase64(e))
        : globalThis.Array.isArray(object?.inclusion_proof)
        ? object.inclusion_proof.map((e: any) => bytesFromBase64(e))
        : [],
      server_signature: isSet(object.serverSignature)
        ? bytesFromBase64(object.serverSignature)
        : isSet(object.server_signature)
        ? bytesFromBase64(object.server_signature)
        : new Uint8Array(0),
    };
  },

  toJSON(message: Receipt): unknown {
    const obj: any = {};
    if (message.content_id !== "") {
      obj.contentId = message.content_id;
    }
    if (message.server_id !== "") {
      obj.serverId = message.server_id;
    }
    if (message.server_key.length !== 0) {
      obj.serverKey = base64FromBytes(message.server_key);
    }
    if (message.accepted_at_ms !== 0n) {
      obj.acceptedAtMs = message.accepted_at_ms.toString();
    }
    if (message.log_index !== 0n) {
      obj.logIndex = message.log_index.toString();
    }
    if (message.sth !== undefined) {
      obj.sth = SignedTreeHead.toJSON(message.sth);
    }
    if (message.inclusion_proof?.length) {
      obj.inclusionProof = message.inclusion_proof.map((e) => base64FromBytes(e));
    }
    if (message.server_signature.length !== 0) {
      obj.serverSignature = base64FromBytes(message.server_signature);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Receipt>, I>>(base?: I): Receipt {
    return Receipt.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Receipt>, I>>(object: I): Receipt {
    const message = createBaseReceipt();
    message.content_id = object.content_id ?? "";
    message.server_id = object.server_id ?? "";
    message.server_key = object.server_key ?? new Uint8Array(0);
    message.accepted_at_ms = (object.accepted_at_ms !== undefined && object.accepted_at_ms !== null)
      ? BigInt(object.accepted_at_ms)
      : 0n;
    message.log_index = (object.log_index !== undefined && object.log_index !== null) ? BigInt(object.log_index) : 0n;
    message.sth = (object.sth !== undefined && object.sth !== null)
      ? SignedTreeHead.fromPartial(object.sth)
      : undefined;
    message.inclusion_proof = object.inclusion_proof?.map((e) => e) || [];
    message.server_signature = object.server_signature ?? new Uint8Array(0);
    return message;
  },
};

function createBaseSignedTreeHead(): SignedTreeHead {
  return {
    server_key: new Uint8Array(0),
    tree_size: 0n,
    root_hash: new Uint8Array(0),
    timestamp_ms: 0n,
    signature: new Uint8Array(0),
  };
}

export const SignedTreeHead: MessageFns<SignedTreeHead> = {
  encode(message: SignedTreeHead, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.server_key.length !== 0) {
      writer.uint32(10).bytes(message.server_key);
    }
    if (message.tree_size !== 0n) {
      if (BigInt.asUintN(64, message.tree_size) !== message.tree_size) {
        throw new globalThis.Error("value provided for field message.tree_size of type uint64 too large");
      }
      writer.uint32(16).uint64(message.tree_size);
    }
    if (message.root_hash.length !== 0) {
      writer.uint32(26).bytes(message.root_hash);
    }
    if (message.timestamp_ms !== 0n) {
      if (BigInt.asIntN(64, message.timestamp_ms) !== message.timestamp_ms) {
        throw new globalThis.Error("value provided for field message.timestamp_ms of type int64 too large");
      }
      writer.uint32(32).int64(message.timestamp_ms);
    }
    if (message.signature.length !== 0) {
      writer.uint32(42).bytes(message.signature);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SignedTreeHead {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSignedTreeHead();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.server_key = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.tree_size = reader.uint64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.root_hash = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.timestamp_ms = reader.int64() as bigint;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.signature = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SignedTreeHead {
    return {
      server_key: isSet(object.serverKey)
        ? bytesFromBase64(object.serverKey)
        : isSet(object.server_key)
        ? bytesFromBase64(object.server_key)
        : new Uint8Array(0),
      tree_size: isSet(object.treeSize)
        ? BigInt(object.treeSize)
        : isSet(object.tree_size)
        ? BigInt(object.tree_size)
        : 0n,
      root_hash: isSet(object.rootHash)
        ? bytesFromBase64(object.rootHash)
        : isSet(object.root_hash)
        ? bytesFromBase64(object.root_hash)
        : new Uint8Array(0),
      timestamp_ms: isSet(object.timestampMs)
        ? BigInt(object.timestampMs)
        : isSet(object.timestamp_ms)
        ? BigInt(object.timestamp_ms)
        : 0n,
      signature: isSet(object.signature) ? bytesFromBase64(object.signature) : new Uint8Array(0),
    };
  },

  toJSON(message: SignedTreeHead): unknown {
    const obj: any = {};
    if (message.server_key.length !== 0) {
      obj.serverKey = base64FromBytes(message.server_key);
    }
    if (message.tree_size !== 0n) {
      obj.treeSize = message.tree_size.toString();
    }
    if (message.root_hash.length !== 0) {
      obj.rootHash = base64FromBytes(message.root_hash);
    }
    if (message.timestamp_ms !== 0n) {
      obj.timestampMs = message.timestamp_ms.toString();
    }
    if (message.signature.length !== 0) {
      obj.signature = base64FromBytes(message.signature);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SignedTreeHead>, I>>(base?: I): SignedTreeHead {
    return SignedTreeHead.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SignedTreeHead>, I>>(object: I): SignedTreeHead {
    const message = createBaseSignedTreeHead();
    message.server_key = object.server_key ?? new Uint8Array(0);
    message.tree_size = (object.tree_size !== undefined && object.tree_size !== null) ? BigInt(object.tree_size) : 0n;
    message.root_hash = object.root_hash ?? new Uint8Array(0);
    message.timestamp_ms = (object.timestamp_ms !== undefined && object.timestamp_ms !== null)
      ? BigInt(object.timestamp_ms)
      : 0n;
    message.signature = object.signature ?? new Uint8Array(0);
    return message;
  },
};

function createBaseEnvelopeError(): EnvelopeError {
  return { code: 0, detail: "", field: "", retry_after_ms: 0n, challenge: undefined };
}

export const EnvelopeError: MessageFns<EnvelopeError> = {
  encode(message: EnvelopeError, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    if (message.detail !== "") {
      writer.uint32(18).string(message.detail);
    }
    if (message.field !== "") {
      writer.uint32(26).string(message.field);
    }
    if (message.retry_after_ms !== 0n) {
      if (BigInt.asIntN(64, message.retry_after_ms) !== message.retry_after_ms) {
        throw new globalThis.Error("value provided for field message.retry_after_ms of type int64 too large");
      }
      writer.uint32(32).int64(message.retry_after_ms);
    }
    if (message.challenge !== undefined) {
      PowChallenge.encode(message.challenge, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EnvelopeError {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEnvelopeError();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.detail = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.field = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.retry_after_ms = reader.int64() as bigint;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.challenge = PowChallenge.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EnvelopeError {
    return {
      code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0,
      detail: isSet(object.detail) ? globalThis.String(object.detail) : "",
      field: isSet(object.field) ? globalThis.String(object.field) : "",
      retry_after_ms: isSet(object.retryAfterMs)
        ? BigInt(object.retryAfterMs)
        : isSet(object.retry_after_ms)
        ? BigInt(object.retry_after_ms)
        : 0n,
      challenge: isSet(object.challenge) ? PowChallenge.fromJSON(object.challenge) : undefined,
    };
  },

  toJSON(message: EnvelopeError): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    if (message.detail !== "") {
      obj.detail = message.detail;
    }
    if (message.field !== "") {
      obj.field = message.field;
    }
    if (message.retry_after_ms !== 0n) {
      obj.retryAfterMs = message.retry_after_ms.toString();
    }
    if (message.challenge !== undefined) {
      obj.challenge = PowChallenge.toJSON(message.challenge);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EnvelopeError>, I>>(base?: I): EnvelopeError {
    return EnvelopeError.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EnvelopeError>, I>>(object: I): EnvelopeError {
    const message = createBaseEnvelopeError();
    message.code = object.code ?? 0;
    message.detail = object.detail ?? "";
    message.field = object.field ?? "";
    message.retry_after_ms = (object.retry_after_ms !== undefined && object.retry_after_ms !== null)
      ? BigInt(object.retry_after_ms)
      : 0n;
    message.challenge = (object.challenge !== undefined && object.challenge !== null)
      ? PowChallenge.fromPartial(object.challenge)
      : undefined;
    return message;
  },
};

function createBasePowChallenge(): PowChallenge {
  return {
    challenge: "",
    algorithm: "",
    memory_kib: 0,
    iterations: 0,
    parallelism: 0,
    bound_to: new Uint8Array(0),
    expires_at_ms: 0n,
  };
}

export const PowChallenge: MessageFns<PowChallenge> = {
  encode(message: PowChallenge, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.challenge !== "") {
      writer.uint32(10).string(message.challenge);
    }
    if (message.algorithm !== "") {
      writer.uint32(18).string(message.algorithm);
    }
    if (message.memory_kib !== 0) {
      writer.uint32(24).uint32(message.memory_kib);
    }
    if (message.iterations !== 0) {
      writer.uint32(32).uint32(message.iterations);
    }
    if (message.parallelism !== 0) {
      writer.uint32(40).uint32(message.parallelism);
    }
    if (message.bound_to.length !== 0) {
      writer.uint32(50).bytes(message.bound_to);
    }
    if (message.expires_at_ms !== 0n) {
      if (BigInt.asIntN(64, message.expires_at_ms) !== message.expires_at_ms) {
        throw new globalThis.Error("value provided for field message.expires_at_ms of type int64 too large");
      }
      writer.uint32(56).int64(message.expires_at_ms);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PowChallenge {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePowChallenge();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.challenge = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.algorithm = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.memory_kib = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.iterations = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.parallelism = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.bound_to = reader.bytes();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.expires_at_ms = reader.int64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PowChallenge {
    return {
      challenge: isSet(object.challenge) ? globalThis.String(object.challenge) : "",
      algorithm: isSet(object.algorithm) ? globalThis.String(object.algorithm) : "",
      memory_kib: isSet(object.memoryKib)
        ? globalThis.Number(object.memoryKib)
        : isSet(object.memory_kib)
        ? globalThis.Number(object.memory_kib)
        : 0,
      iterations: isSet(object.iterations) ? globalThis.Number(object.iterations) : 0,
      parallelism: isSet(object.parallelism) ? globalThis.Number(object.parallelism) : 0,
      bound_to: isSet(object.boundTo)
        ? bytesFromBase64(object.boundTo)
        : isSet(object.bound_to)
        ? bytesFromBase64(object.bound_to)
        : new Uint8Array(0),
      expires_at_ms: isSet(object.expiresAtMs)
        ? BigInt(object.expiresAtMs)
        : isSet(object.expires_at_ms)
        ? BigInt(object.expires_at_ms)
        : 0n,
    };
  },

  toJSON(message: PowChallenge): unknown {
    const obj: any = {};
    if (message.challenge !== "") {
      obj.challenge = message.challenge;
    }
    if (message.algorithm !== "") {
      obj.algorithm = message.algorithm;
    }
    if (message.memory_kib !== 0) {
      obj.memoryKib = Math.round(message.memory_kib);
    }
    if (message.iterations !== 0) {
      obj.iterations = Math.round(message.iterations);
    }
    if (message.parallelism !== 0) {
      obj.parallelism = Math.round(message.parallelism);
    }
    if (message.bound_to.length !== 0) {
      obj.boundTo = base64FromBytes(message.bound_to);
    }
    if (message.expires_at_ms !== 0n) {
      obj.expiresAtMs = message.expires_at_ms.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PowChallenge>, I>>(base?: I): PowChallenge {
    return PowChallenge.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PowChallenge>, I>>(object: I): PowChallenge {
    const message = createBasePowChallenge();
    message.challenge = object.challenge ?? "";
    message.algorithm = object.algorithm ?? "";
    message.memory_kib = object.memory_kib ?? 0;
    message.iterations = object.iterations ?? 0;
    message.parallelism = object.parallelism ?? 0;
    message.bound_to = object.bound_to ?? new Uint8Array(0);
    message.expires_at_ms = (object.expires_at_ms !== undefined && object.expires_at_ms !== null)
      ? BigInt(object.expires_at_ms)
      : 0n;
    return message;
  },
};

function createBasePowSolution(): PowSolution {
  return { challenge: "", solution: new Uint8Array(0), bound_to: new Uint8Array(0) };
}

export const PowSolution: MessageFns<PowSolution> = {
  encode(message: PowSolution, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.challenge !== "") {
      writer.uint32(10).string(message.challenge);
    }
    if (message.solution.length !== 0) {
      writer.uint32(18).bytes(message.solution);
    }
    if (message.bound_to.length !== 0) {
      writer.uint32(26).bytes(message.bound_to);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PowSolution {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePowSolution();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.challenge = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.solution = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.bound_to = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PowSolution {
    return {
      challenge: isSet(object.challenge) ? globalThis.String(object.challenge) : "",
      solution: isSet(object.solution) ? bytesFromBase64(object.solution) : new Uint8Array(0),
      bound_to: isSet(object.boundTo)
        ? bytesFromBase64(object.boundTo)
        : isSet(object.bound_to)
        ? bytesFromBase64(object.bound_to)
        : new Uint8Array(0),
    };
  },

  toJSON(message: PowSolution): unknown {
    const obj: any = {};
    if (message.challenge !== "") {
      obj.challenge = message.challenge;
    }
    if (message.solution.length !== 0) {
      obj.solution = base64FromBytes(message.solution);
    }
    if (message.bound_to.length !== 0) {
      obj.boundTo = base64FromBytes(message.bound_to);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PowSolution>, I>>(base?: I): PowSolution {
    return PowSolution.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PowSolution>, I>>(object: I): PowSolution {
    const message = createBasePowSolution();
    message.challenge = object.challenge ?? "";
    message.solution = object.solution ?? new Uint8Array(0);
    message.bound_to = object.bound_to ?? new Uint8Array(0);
    return message;
  },
};

function createBaseKeyCertificate(): KeyCertificate {
  return {
    plane: 0,
    device_key: new Uint8Array(0),
    pq_key: new Uint8Array(0),
    pq_attestation: new Uint8Array(0),
    valid_from: 0n,
    valid_until: 0n,
    self_signature: new Uint8Array(0),
  };
}

export const KeyCertificate: MessageFns<KeyCertificate> = {
  encode(message: KeyCertificate, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.plane !== 0) {
      writer.uint32(8).int32(message.plane);
    }
    if (message.device_key.length !== 0) {
      writer.uint32(18).bytes(message.device_key);
    }
    if (message.pq_key.length !== 0) {
      writer.uint32(26).bytes(message.pq_key);
    }
    if (message.pq_attestation.length !== 0) {
      writer.uint32(34).bytes(message.pq_attestation);
    }
    if (message.valid_from !== 0n) {
      if (BigInt.asIntN(64, message.valid_from) !== message.valid_from) {
        throw new globalThis.Error("value provided for field message.valid_from of type int64 too large");
      }
      writer.uint32(40).int64(message.valid_from);
    }
    if (message.valid_until !== 0n) {
      if (BigInt.asIntN(64, message.valid_until) !== message.valid_until) {
        throw new globalThis.Error("value provided for field message.valid_until of type int64 too large");
      }
      writer.uint32(48).int64(message.valid_until);
    }
    if (message.self_signature.length !== 0) {
      writer.uint32(58).bytes(message.self_signature);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KeyCertificate {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKeyCertificate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.plane = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.device_key = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.pq_key = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.pq_attestation = reader.bytes();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.valid_from = reader.int64() as bigint;
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.valid_until = reader.int64() as bigint;
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.self_signature = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KeyCertificate {
    return {
      plane: isSet(object.plane) ? planeFromJSON(object.plane) : 0,
      device_key: isSet(object.deviceKey)
        ? bytesFromBase64(object.deviceKey)
        : isSet(object.device_key)
        ? bytesFromBase64(object.device_key)
        : new Uint8Array(0),
      pq_key: isSet(object.pqKey)
        ? bytesFromBase64(object.pqKey)
        : isSet(object.pq_key)
        ? bytesFromBase64(object.pq_key)
        : new Uint8Array(0),
      pq_attestation: isSet(object.pqAttestation)
        ? bytesFromBase64(object.pqAttestation)
        : isSet(object.pq_attestation)
        ? bytesFromBase64(object.pq_attestation)
        : new Uint8Array(0),
      valid_from: isSet(object.validFrom)
        ? BigInt(object.validFrom)
        : isSet(object.valid_from)
        ? BigInt(object.valid_from)
        : 0n,
      valid_until: isSet(object.validUntil)
        ? BigInt(object.validUntil)
        : isSet(object.valid_until)
        ? BigInt(object.valid_until)
        : 0n,
      self_signature: isSet(object.selfSignature)
        ? bytesFromBase64(object.selfSignature)
        : isSet(object.self_signature)
        ? bytesFromBase64(object.self_signature)
        : new Uint8Array(0),
    };
  },

  toJSON(message: KeyCertificate): unknown {
    const obj: any = {};
    if (message.plane !== 0) {
      obj.plane = planeToJSON(message.plane);
    }
    if (message.device_key.length !== 0) {
      obj.deviceKey = base64FromBytes(message.device_key);
    }
    if (message.pq_key.length !== 0) {
      obj.pqKey = base64FromBytes(message.pq_key);
    }
    if (message.pq_attestation.length !== 0) {
      obj.pqAttestation = base64FromBytes(message.pq_attestation);
    }
    if (message.valid_from !== 0n) {
      obj.validFrom = message.valid_from.toString();
    }
    if (message.valid_until !== 0n) {
      obj.validUntil = message.valid_until.toString();
    }
    if (message.self_signature.length !== 0) {
      obj.selfSignature = base64FromBytes(message.self_signature);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<KeyCertificate>, I>>(base?: I): KeyCertificate {
    return KeyCertificate.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KeyCertificate>, I>>(object: I): KeyCertificate {
    const message = createBaseKeyCertificate();
    message.plane = object.plane ?? 0;
    message.device_key = object.device_key ?? new Uint8Array(0);
    message.pq_key = object.pq_key ?? new Uint8Array(0);
    message.pq_attestation = object.pq_attestation ?? new Uint8Array(0);
    message.valid_from = (object.valid_from !== undefined && object.valid_from !== null)
      ? BigInt(object.valid_from)
      : 0n;
    message.valid_until = (object.valid_until !== undefined && object.valid_until !== null)
      ? BigInt(object.valid_until)
      : 0n;
    message.self_signature = object.self_signature ?? new Uint8Array(0);
    return message;
  },
};

function createBaseKeyRevocation(): KeyRevocation {
  return {
    plane: 0,
    revoked_key: new Uint8Array(0),
    kind: 0,
    effective_from_ms: 0n,
    replacement_key: new Uint8Array(0),
  };
}

export const KeyRevocation: MessageFns<KeyRevocation> = {
  encode(message: KeyRevocation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.plane !== 0) {
      writer.uint32(8).int32(message.plane);
    }
    if (message.revoked_key.length !== 0) {
      writer.uint32(18).bytes(message.revoked_key);
    }
    if (message.kind !== 0) {
      writer.uint32(24).int32(message.kind);
    }
    if (message.effective_from_ms !== 0n) {
      if (BigInt.asIntN(64, message.effective_from_ms) !== message.effective_from_ms) {
        throw new globalThis.Error("value provided for field message.effective_from_ms of type int64 too large");
      }
      writer.uint32(32).int64(message.effective_from_ms);
    }
    if (message.replacement_key.length !== 0) {
      writer.uint32(42).bytes(message.replacement_key);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KeyRevocation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKeyRevocation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.plane = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.revoked_key = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.kind = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.effective_from_ms = reader.int64() as bigint;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.replacement_key = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KeyRevocation {
    return {
      plane: isSet(object.plane) ? planeFromJSON(object.plane) : 0,
      revoked_key: isSet(object.revokedKey)
        ? bytesFromBase64(object.revokedKey)
        : isSet(object.revoked_key)
        ? bytesFromBase64(object.revoked_key)
        : new Uint8Array(0),
      kind: isSet(object.kind) ? revocationKindFromJSON(object.kind) : 0,
      effective_from_ms: isSet(object.effectiveFromMs)
        ? BigInt(object.effectiveFromMs)
        : isSet(object.effective_from_ms)
        ? BigInt(object.effective_from_ms)
        : 0n,
      replacement_key: isSet(object.replacementKey)
        ? bytesFromBase64(object.replacementKey)
        : isSet(object.replacement_key)
        ? bytesFromBase64(object.replacement_key)
        : new Uint8Array(0),
    };
  },

  toJSON(message: KeyRevocation): unknown {
    const obj: any = {};
    if (message.plane !== 0) {
      obj.plane = planeToJSON(message.plane);
    }
    if (message.revoked_key.length !== 0) {
      obj.revokedKey = base64FromBytes(message.revoked_key);
    }
    if (message.kind !== 0) {
      obj.kind = revocationKindToJSON(message.kind);
    }
    if (message.effective_from_ms !== 0n) {
      obj.effectiveFromMs = message.effective_from_ms.toString();
    }
    if (message.replacement_key.length !== 0) {
      obj.replacementKey = base64FromBytes(message.replacement_key);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<KeyRevocation>, I>>(base?: I): KeyRevocation {
    return KeyRevocation.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KeyRevocation>, I>>(object: I): KeyRevocation {
    const message = createBaseKeyRevocation();
    message.plane = object.plane ?? 0;
    message.revoked_key = object.revoked_key ?? new Uint8Array(0);
    message.kind = object.kind ?? 0;
    message.effective_from_ms = (object.effective_from_ms !== undefined && object.effective_from_ms !== null)
      ? BigInt(object.effective_from_ms)
      : 0n;
    message.replacement_key = object.replacement_key ?? new Uint8Array(0);
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

type DeepPartial<T> = T extends bigint ? string | number | bigint
  : T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
