// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: jagoo/v1/federation.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  Envelope,
  ErrorCode,
  errorCodeFromJSON,
  errorCodeToJSON,
  Plane,
  planeFromJSON,
  planeToJSON,
  Priority,
  priorityFromJSON,
  priorityToJSON,
  SignedTreeHead,
} from "./envelope";
import { PeerEndpoint, PeerRecord, Quota, TrustLevel, trustLevelFromJSON, trustLevelToJSON } from "./transport";

/**
 * ─────────────────────────────────────────────────────────────────────────────
 * Handshake
 * ─────────────────────────────────────────────────────────────────────────────
 */
export interface AnnounceRequest {
  server_key: Uint8Array;
  display_name: string;
  software: string;
  version: string;
  endpoints: PeerEndpoint[];
  communities: string[];
  channels: string[];
  /** FED-17: which planes this node serves. */
  planes: Plane[];
  accepted_classes: Priority[];
  current_sth?: SignedTreeHead | undefined;
  timestamp_ms: bigint;
  nonce: Uint8Array;
  /** Over fields 1..12. */
  signature: Uint8Array;
}

export interface AnnounceResponse {
  server_key: Uint8Array;
  assigned: TrustLevel;
  endpoints: PeerEndpoint[];
  vouches: ServerVouch[];
  granted_quota?: Quota | undefined;
  current_sth?: SignedTreeHead | undefined;
  signature: Uint8Array;
}

/**
 * ─────────────────────────────────────────────────────────────────────────────
 * Delivery
 *
 * FD-04: inbound envelopes MUST be PROJECTED into the read model, not merely archived.
 * v1's receive() inserted into a federationactivities collection that nothing ever
 * read, and emitLocalActivity() wrote to an outbox that was never delivered.
 * Federation was a message morgue.
 * ─────────────────────────────────────────────────────────────────────────────
 */
export interface DeliverAck {
  /** content_ids */
  accepted: string[];
  rejected: Rejection[];
  backpressure_hint_ms: number;
  our_log_size: bigint;
}

export interface Rejection {
  content_id: string;
  code: ErrorCode;
  detail: string;
}

export interface StreamRequest {
  communities: string[];
  channels: string[];
  planes: Plane[];
  classes: Priority[];
  /** NFR-R07: durable, so a restart does not lose position. */
  since_index: bigint;
}

export interface BackfillRequest {
  communities: string[];
  channels: string[];
  planes: Plane[];
  from_index: bigint;
  to_index: bigint;
  max: number;
}

/**
 * ─────────────────────────────────────────────────────────────────────────────
 * Trust — TOFU plus web-of-trust (Plans/05 §3)
 * ─────────────────────────────────────────────────────────────────────────────
 */
export interface ServerVouch {
  peer_key: Uint8Array;
  level: TrustLevel;
  note: string;
  asserted_at_ms: bigint;
  signature: Uint8Array;
}

/**
 * ─────────────────────────────────────────────────────────────────────────────
 * Transparency gossip
 *
 * FD-09: an inconsistent peer STH means the peer rewrote its log. The node MUST record
 * the divergence, alert the operator, and demote the peer to BLOCKED pending review.
 * FD-10: PeerObservation lets node C learn that A saw a different STH for B than C did,
 * catching a peer that shows different logs to different partners.
 * ─────────────────────────────────────────────────────────────────────────────
 */
export interface TreeHeadExchange {
  server_key: Uint8Array;
  sth?: SignedTreeHead | undefined;
  observed: PeerObservation[];
  signature: Uint8Array;
}

export interface PeerObservation {
  peer_key: Uint8Array;
  sth?: SignedTreeHead | undefined;
  observed_at_ms: bigint;
}

/**
 * ─────────────────────────────────────────────────────────────────────────────
 * Directory — heavily used by P3 for pre-positioning (TP-05)
 * ─────────────────────────────────────────────────────────────────────────────
 */
export interface DirectoryExchange {
  peers: PeerRecord[];
  generated_at_ms: bigint;
  signature: Uint8Array;
}

function createBaseAnnounceRequest(): AnnounceRequest {
  return {
    server_key: new Uint8Array(0),
    display_name: "",
    software: "",
    version: "",
    endpoints: [],
    communities: [],
    channels: [],
    planes: [],
    accepted_classes: [],
    current_sth: undefined,
    timestamp_ms: 0n,
    nonce: new Uint8Array(0),
    signature: new Uint8Array(0),
  };
}

export const AnnounceRequest: MessageFns<AnnounceRequest> = {
  encode(message: AnnounceRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.server_key.length !== 0) {
      writer.uint32(10).bytes(message.server_key);
    }
    if (message.display_name !== "") {
      writer.uint32(18).string(message.display_name);
    }
    if (message.software !== "") {
      writer.uint32(26).string(message.software);
    }
    if (message.version !== "") {
      writer.uint32(34).string(message.version);
    }
    for (const v of message.endpoints) {
      PeerEndpoint.encode(v!, writer.uint32(42).fork()).join();
    }
    for (const v of message.communities) {
      writer.uint32(50).string(v!);
    }
    for (const v of message.channels) {
      writer.uint32(58).string(v!);
    }
    writer.uint32(66).fork();
    for (const v of message.planes) {
      writer.int32(v);
    }
    writer.join();
    writer.uint32(74).fork();
    for (const v of message.accepted_classes) {
      writer.int32(v);
    }
    writer.join();
    if (message.current_sth !== undefined) {
      SignedTreeHead.encode(message.current_sth, writer.uint32(82).fork()).join();
    }
    if (message.timestamp_ms !== 0n) {
      if (BigInt.asIntN(64, message.timestamp_ms) !== message.timestamp_ms) {
        throw new globalThis.Error("value provided for field message.timestamp_ms of type int64 too large");
      }
      writer.uint32(88).int64(message.timestamp_ms);
    }
    if (message.nonce.length !== 0) {
      writer.uint32(98).bytes(message.nonce);
    }
    if (message.signature.length !== 0) {
      writer.uint32(106).bytes(message.signature);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AnnounceRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAnnounceRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.server_key = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.display_name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.software = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.version = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.endpoints.push(PeerEndpoint.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.communities.push(reader.string());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.channels.push(reader.string());
          continue;
        }
        case 8: {
          if (tag === 64) {
            message.planes.push(reader.int32() as any);

            continue;
          }

          if (tag === 66) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.planes.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
        case 9: {
          if (tag === 72) {
            message.accepted_classes.push(reader.int32() as any);

            continue;
          }

          if (tag === 74) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.accepted_classes.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.current_sth = SignedTreeHead.decode(reader, reader.uint32());
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.timestamp_ms = reader.int64() as bigint;
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.nonce = reader.bytes();
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.signature = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AnnounceRequest {
    return {
      server_key: isSet(object.serverKey)
        ? bytesFromBase64(object.serverKey)
        : isSet(object.server_key)
        ? bytesFromBase64(object.server_key)
        : new Uint8Array(0),
      display_name: isSet(object.displayName)
        ? globalThis.String(object.displayName)
        : isSet(object.display_name)
        ? globalThis.String(object.display_name)
        : "",
      software: isSet(object.software) ? globalThis.String(object.software) : "",
      version: isSet(object.version) ? globalThis.String(object.version) : "",
      endpoints: globalThis.Array.isArray(object?.endpoints)
        ? object.endpoints.map((e: any) => PeerEndpoint.fromJSON(e))
        : [],
      communities: globalThis.Array.isArray(object?.communities)
        ? object.communities.map((e: any) => globalThis.String(e))
        : [],
      channels: globalThis.Array.isArray(object?.channels)
        ? object.channels.map((e: any) => globalThis.String(e))
        : [],
      planes: globalThis.Array.isArray(object?.planes) ? object.planes.map((e: any) => planeFromJSON(e)) : [],
      accepted_classes: globalThis.Array.isArray(object?.acceptedClasses)
        ? object.acceptedClasses.map((e: any) => priorityFromJSON(e))
        : globalThis.Array.isArray(object?.accepted_classes)
        ? object.accepted_classes.map((e: any) => priorityFromJSON(e))
        : [],
      current_sth: isSet(object.currentSth)
        ? SignedTreeHead.fromJSON(object.currentSth)
        : isSet(object.current_sth)
        ? SignedTreeHead.fromJSON(object.current_sth)
        : undefined,
      timestamp_ms: isSet(object.timestampMs)
        ? BigInt(object.timestampMs)
        : isSet(object.timestamp_ms)
        ? BigInt(object.timestamp_ms)
        : 0n,
      nonce: isSet(object.nonce) ? bytesFromBase64(object.nonce) : new Uint8Array(0),
      signature: isSet(object.signature) ? bytesFromBase64(object.signature) : new Uint8Array(0),
    };
  },

  toJSON(message: AnnounceRequest): unknown {
    const obj: any = {};
    if (message.server_key.length !== 0) {
      obj.serverKey = base64FromBytes(message.server_key);
    }
    if (message.display_name !== "") {
      obj.displayName = message.display_name;
    }
    if (message.software !== "") {
      obj.software = message.software;
    }
    if (message.version !== "") {
      obj.version = message.version;
    }
    if (message.endpoints?.length) {
      obj.endpoints = message.endpoints.map((e) => PeerEndpoint.toJSON(e));
    }
    if (message.communities?.length) {
      obj.communities = message.communities;
    }
    if (message.channels?.length) {
      obj.channels = message.channels;
    }
    if (message.planes?.length) {
      obj.planes = message.planes.map((e) => planeToJSON(e));
    }
    if (message.accepted_classes?.length) {
      obj.acceptedClasses = message.accepted_classes.map((e) => priorityToJSON(e));
    }
    if (message.current_sth !== undefined) {
      obj.currentSth = SignedTreeHead.toJSON(message.current_sth);
    }
    if (message.timestamp_ms !== 0n) {
      obj.timestampMs = message.timestamp_ms.toString();
    }
    if (message.nonce.length !== 0) {
      obj.nonce = base64FromBytes(message.nonce);
    }
    if (message.signature.length !== 0) {
      obj.signature = base64FromBytes(message.signature);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AnnounceRequest>, I>>(base?: I): AnnounceRequest {
    return AnnounceRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AnnounceRequest>, I>>(object: I): AnnounceRequest {
    const message = createBaseAnnounceRequest();
    message.server_key = object.server_key ?? new Uint8Array(0);
    message.display_name = object.display_name ?? "";
    message.software = object.software ?? "";
    message.version = object.version ?? "";
    message.endpoints = object.endpoints?.map((e) => PeerEndpoint.fromPartial(e)) || [];
    message.communities = object.communities?.map((e) => e) || [];
    message.channels = object.channels?.map((e) => e) || [];
    message.planes = object.planes?.map((e) => e) || [];
    message.accepted_classes = object.accepted_classes?.map((e) => e) || [];
    message.current_sth = (object.current_sth !== undefined && object.current_sth !== null)
      ? SignedTreeHead.fromPartial(object.current_sth)
      : undefined;
    message.timestamp_ms = (object.timestamp_ms !== undefined && object.timestamp_ms !== null)
      ? BigInt(object.timestamp_ms)
      : 0n;
    message.nonce = object.nonce ?? new Uint8Array(0);
    message.signature = object.signature ?? new Uint8Array(0);
    return message;
  },
};

function createBaseAnnounceResponse(): AnnounceResponse {
  return {
    server_key: new Uint8Array(0),
    assigned: 0,
    endpoints: [],
    vouches: [],
    granted_quota: undefined,
    current_sth: undefined,
    signature: new Uint8Array(0),
  };
}

export const AnnounceResponse: MessageFns<AnnounceResponse> = {
  encode(message: AnnounceResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.server_key.length !== 0) {
      writer.uint32(10).bytes(message.server_key);
    }
    if (message.assigned !== 0) {
      writer.uint32(16).int32(message.assigned);
    }
    for (const v of message.endpoints) {
      PeerEndpoint.encode(v!, writer.uint32(26).fork()).join();
    }
    for (const v of message.vouches) {
      ServerVouch.encode(v!, writer.uint32(34).fork()).join();
    }
    if (message.granted_quota !== undefined) {
      Quota.encode(message.granted_quota, writer.uint32(42).fork()).join();
    }
    if (message.current_sth !== undefined) {
      SignedTreeHead.encode(message.current_sth, writer.uint32(50).fork()).join();
    }
    if (message.signature.length !== 0) {
      writer.uint32(58).bytes(message.signature);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AnnounceResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAnnounceResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.server_key = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.assigned = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.endpoints.push(PeerEndpoint.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.vouches.push(ServerVouch.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.granted_quota = Quota.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.current_sth = SignedTreeHead.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.signature = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AnnounceResponse {
    return {
      server_key: isSet(object.serverKey)
        ? bytesFromBase64(object.serverKey)
        : isSet(object.server_key)
        ? bytesFromBase64(object.server_key)
        : new Uint8Array(0),
      assigned: isSet(object.assigned) ? trustLevelFromJSON(object.assigned) : 0,
      endpoints: globalThis.Array.isArray(object?.endpoints)
        ? object.endpoints.map((e: any) => PeerEndpoint.fromJSON(e))
        : [],
      vouches: globalThis.Array.isArray(object?.vouches) ? object.vouches.map((e: any) => ServerVouch.fromJSON(e)) : [],
      granted_quota: isSet(object.grantedQuota)
        ? Quota.fromJSON(object.grantedQuota)
        : isSet(object.granted_quota)
        ? Quota.fromJSON(object.granted_quota)
        : undefined,
      current_sth: isSet(object.currentSth)
        ? SignedTreeHead.fromJSON(object.currentSth)
        : isSet(object.current_sth)
        ? SignedTreeHead.fromJSON(object.current_sth)
        : undefined,
      signature: isSet(object.signature) ? bytesFromBase64(object.signature) : new Uint8Array(0),
    };
  },

  toJSON(message: AnnounceResponse): unknown {
    const obj: any = {};
    if (message.server_key.length !== 0) {
      obj.serverKey = base64FromBytes(message.server_key);
    }
    if (message.assigned !== 0) {
      obj.assigned = trustLevelToJSON(message.assigned);
    }
    if (message.endpoints?.length) {
      obj.endpoints = message.endpoints.map((e) => PeerEndpoint.toJSON(e));
    }
    if (message.vouches?.length) {
      obj.vouches = message.vouches.map((e) => ServerVouch.toJSON(e));
    }
    if (message.granted_quota !== undefined) {
      obj.grantedQuota = Quota.toJSON(message.granted_quota);
    }
    if (message.current_sth !== undefined) {
      obj.currentSth = SignedTreeHead.toJSON(message.current_sth);
    }
    if (message.signature.length !== 0) {
      obj.signature = base64FromBytes(message.signature);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AnnounceResponse>, I>>(base?: I): AnnounceResponse {
    return AnnounceResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AnnounceResponse>, I>>(object: I): AnnounceResponse {
    const message = createBaseAnnounceResponse();
    message.server_key = object.server_key ?? new Uint8Array(0);
    message.assigned = object.assigned ?? 0;
    message.endpoints = object.endpoints?.map((e) => PeerEndpoint.fromPartial(e)) || [];
    message.vouches = object.vouches?.map((e) => ServerVouch.fromPartial(e)) || [];
    message.granted_quota = (object.granted_quota !== undefined && object.granted_quota !== null)
      ? Quota.fromPartial(object.granted_quota)
      : undefined;
    message.current_sth = (object.current_sth !== undefined && object.current_sth !== null)
      ? SignedTreeHead.fromPartial(object.current_sth)
      : undefined;
    message.signature = object.signature ?? new Uint8Array(0);
    return message;
  },
};

function createBaseDeliverAck(): DeliverAck {
  return { accepted: [], rejected: [], backpressure_hint_ms: 0, our_log_size: 0n };
}

export const DeliverAck: MessageFns<DeliverAck> = {
  encode(message: DeliverAck, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.accepted) {
      writer.uint32(10).string(v!);
    }
    for (const v of message.rejected) {
      Rejection.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.backpressure_hint_ms !== 0) {
      writer.uint32(24).uint32(message.backpressure_hint_ms);
    }
    if (message.our_log_size !== 0n) {
      if (BigInt.asUintN(64, message.our_log_size) !== message.our_log_size) {
        throw new globalThis.Error("value provided for field message.our_log_size of type uint64 too large");
      }
      writer.uint32(32).uint64(message.our_log_size);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeliverAck {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeliverAck();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.accepted.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.rejected.push(Rejection.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.backpressure_hint_ms = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.our_log_size = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeliverAck {
    return {
      accepted: globalThis.Array.isArray(object?.accepted) ? object.accepted.map((e: any) => globalThis.String(e)) : [],
      rejected: globalThis.Array.isArray(object?.rejected)
        ? object.rejected.map((e: any) => Rejection.fromJSON(e))
        : [],
      backpressure_hint_ms: isSet(object.backpressureHintMs)
        ? globalThis.Number(object.backpressureHintMs)
        : isSet(object.backpressure_hint_ms)
        ? globalThis.Number(object.backpressure_hint_ms)
        : 0,
      our_log_size: isSet(object.ourLogSize)
        ? BigInt(object.ourLogSize)
        : isSet(object.our_log_size)
        ? BigInt(object.our_log_size)
        : 0n,
    };
  },

  toJSON(message: DeliverAck): unknown {
    const obj: any = {};
    if (message.accepted?.length) {
      obj.accepted = message.accepted;
    }
    if (message.rejected?.length) {
      obj.rejected = message.rejected.map((e) => Rejection.toJSON(e));
    }
    if (message.backpressure_hint_ms !== 0) {
      obj.backpressureHintMs = Math.round(message.backpressure_hint_ms);
    }
    if (message.our_log_size !== 0n) {
      obj.ourLogSize = message.our_log_size.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeliverAck>, I>>(base?: I): DeliverAck {
    return DeliverAck.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeliverAck>, I>>(object: I): DeliverAck {
    const message = createBaseDeliverAck();
    message.accepted = object.accepted?.map((e) => e) || [];
    message.rejected = object.rejected?.map((e) => Rejection.fromPartial(e)) || [];
    message.backpressure_hint_ms = object.backpressure_hint_ms ?? 0;
    message.our_log_size = (object.our_log_size !== undefined && object.our_log_size !== null)
      ? BigInt(object.our_log_size)
      : 0n;
    return message;
  },
};

function createBaseRejection(): Rejection {
  return { content_id: "", code: 0, detail: "" };
}

export const Rejection: MessageFns<Rejection> = {
  encode(message: Rejection, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.content_id !== "") {
      writer.uint32(10).string(message.content_id);
    }
    if (message.code !== 0) {
      writer.uint32(16).int32(message.code);
    }
    if (message.detail !== "") {
      writer.uint32(26).string(message.detail);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Rejection {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRejection();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.content_id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.detail = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Rejection {
    return {
      content_id: isSet(object.contentId)
        ? globalThis.String(object.contentId)
        : isSet(object.content_id)
        ? globalThis.String(object.content_id)
        : "",
      code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0,
      detail: isSet(object.detail) ? globalThis.String(object.detail) : "",
    };
  },

  toJSON(message: Rejection): unknown {
    const obj: any = {};
    if (message.content_id !== "") {
      obj.contentId = message.content_id;
    }
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    if (message.detail !== "") {
      obj.detail = message.detail;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Rejection>, I>>(base?: I): Rejection {
    return Rejection.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Rejection>, I>>(object: I): Rejection {
    const message = createBaseRejection();
    message.content_id = object.content_id ?? "";
    message.code = object.code ?? 0;
    message.detail = object.detail ?? "";
    return message;
  },
};

function createBaseStreamRequest(): StreamRequest {
  return { communities: [], channels: [], planes: [], classes: [], since_index: 0n };
}

export const StreamRequest: MessageFns<StreamRequest> = {
  encode(message: StreamRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.communities) {
      writer.uint32(10).string(v!);
    }
    for (const v of message.channels) {
      writer.uint32(18).string(v!);
    }
    writer.uint32(26).fork();
    for (const v of message.planes) {
      writer.int32(v);
    }
    writer.join();
    writer.uint32(34).fork();
    for (const v of message.classes) {
      writer.int32(v);
    }
    writer.join();
    if (message.since_index !== 0n) {
      if (BigInt.asUintN(64, message.since_index) !== message.since_index) {
        throw new globalThis.Error("value provided for field message.since_index of type uint64 too large");
      }
      writer.uint32(40).uint64(message.since_index);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StreamRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStreamRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.communities.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.channels.push(reader.string());
          continue;
        }
        case 3: {
          if (tag === 24) {
            message.planes.push(reader.int32() as any);

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.planes.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
        case 4: {
          if (tag === 32) {
            message.classes.push(reader.int32() as any);

            continue;
          }

          if (tag === 34) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.classes.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.since_index = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StreamRequest {
    return {
      communities: globalThis.Array.isArray(object?.communities)
        ? object.communities.map((e: any) => globalThis.String(e))
        : [],
      channels: globalThis.Array.isArray(object?.channels) ? object.channels.map((e: any) => globalThis.String(e)) : [],
      planes: globalThis.Array.isArray(object?.planes) ? object.planes.map((e: any) => planeFromJSON(e)) : [],
      classes: globalThis.Array.isArray(object?.classes) ? object.classes.map((e: any) => priorityFromJSON(e)) : [],
      since_index: isSet(object.sinceIndex)
        ? BigInt(object.sinceIndex)
        : isSet(object.since_index)
        ? BigInt(object.since_index)
        : 0n,
    };
  },

  toJSON(message: StreamRequest): unknown {
    const obj: any = {};
    if (message.communities?.length) {
      obj.communities = message.communities;
    }
    if (message.channels?.length) {
      obj.channels = message.channels;
    }
    if (message.planes?.length) {
      obj.planes = message.planes.map((e) => planeToJSON(e));
    }
    if (message.classes?.length) {
      obj.classes = message.classes.map((e) => priorityToJSON(e));
    }
    if (message.since_index !== 0n) {
      obj.sinceIndex = message.since_index.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StreamRequest>, I>>(base?: I): StreamRequest {
    return StreamRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StreamRequest>, I>>(object: I): StreamRequest {
    const message = createBaseStreamRequest();
    message.communities = object.communities?.map((e) => e) || [];
    message.channels = object.channels?.map((e) => e) || [];
    message.planes = object.planes?.map((e) => e) || [];
    message.classes = object.classes?.map((e) => e) || [];
    message.since_index = (object.since_index !== undefined && object.since_index !== null)
      ? BigInt(object.since_index)
      : 0n;
    return message;
  },
};

function createBaseBackfillRequest(): BackfillRequest {
  return { communities: [], channels: [], planes: [], from_index: 0n, to_index: 0n, max: 0 };
}

export const BackfillRequest: MessageFns<BackfillRequest> = {
  encode(message: BackfillRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.communities) {
      writer.uint32(10).string(v!);
    }
    for (const v of message.channels) {
      writer.uint32(18).string(v!);
    }
    writer.uint32(26).fork();
    for (const v of message.planes) {
      writer.int32(v);
    }
    writer.join();
    if (message.from_index !== 0n) {
      if (BigInt.asUintN(64, message.from_index) !== message.from_index) {
        throw new globalThis.Error("value provided for field message.from_index of type uint64 too large");
      }
      writer.uint32(32).uint64(message.from_index);
    }
    if (message.to_index !== 0n) {
      if (BigInt.asUintN(64, message.to_index) !== message.to_index) {
        throw new globalThis.Error("value provided for field message.to_index of type uint64 too large");
      }
      writer.uint32(40).uint64(message.to_index);
    }
    if (message.max !== 0) {
      writer.uint32(48).uint32(message.max);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BackfillRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBackfillRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.communities.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.channels.push(reader.string());
          continue;
        }
        case 3: {
          if (tag === 24) {
            message.planes.push(reader.int32() as any);

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.planes.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.from_index = reader.uint64() as bigint;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.to_index = reader.uint64() as bigint;
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.max = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BackfillRequest {
    return {
      communities: globalThis.Array.isArray(object?.communities)
        ? object.communities.map((e: any) => globalThis.String(e))
        : [],
      channels: globalThis.Array.isArray(object?.channels) ? object.channels.map((e: any) => globalThis.String(e)) : [],
      planes: globalThis.Array.isArray(object?.planes) ? object.planes.map((e: any) => planeFromJSON(e)) : [],
      from_index: isSet(object.fromIndex)
        ? BigInt(object.fromIndex)
        : isSet(object.from_index)
        ? BigInt(object.from_index)
        : 0n,
      to_index: isSet(object.toIndex) ? BigInt(object.toIndex) : isSet(object.to_index) ? BigInt(object.to_index) : 0n,
      max: isSet(object.max) ? globalThis.Number(object.max) : 0,
    };
  },

  toJSON(message: BackfillRequest): unknown {
    const obj: any = {};
    if (message.communities?.length) {
      obj.communities = message.communities;
    }
    if (message.channels?.length) {
      obj.channels = message.channels;
    }
    if (message.planes?.length) {
      obj.planes = message.planes.map((e) => planeToJSON(e));
    }
    if (message.from_index !== 0n) {
      obj.fromIndex = message.from_index.toString();
    }
    if (message.to_index !== 0n) {
      obj.toIndex = message.to_index.toString();
    }
    if (message.max !== 0) {
      obj.max = Math.round(message.max);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BackfillRequest>, I>>(base?: I): BackfillRequest {
    return BackfillRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BackfillRequest>, I>>(object: I): BackfillRequest {
    const message = createBaseBackfillRequest();
    message.communities = object.communities?.map((e) => e) || [];
    message.channels = object.channels?.map((e) => e) || [];
    message.planes = object.planes?.map((e) => e) || [];
    message.from_index = (object.from_index !== undefined && object.from_index !== null)
      ? BigInt(object.from_index)
      : 0n;
    message.to_index = (object.to_index !== undefined && object.to_index !== null) ? BigInt(object.to_index) : 0n;
    message.max = object.max ?? 0;
    return message;
  },
};

function createBaseServerVouch(): ServerVouch {
  return { peer_key: new Uint8Array(0), level: 0, note: "", asserted_at_ms: 0n, signature: new Uint8Array(0) };
}

export const ServerVouch: MessageFns<ServerVouch> = {
  encode(message: ServerVouch, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.peer_key.length !== 0) {
      writer.uint32(10).bytes(message.peer_key);
    }
    if (message.level !== 0) {
      writer.uint32(16).int32(message.level);
    }
    if (message.note !== "") {
      writer.uint32(26).string(message.note);
    }
    if (message.asserted_at_ms !== 0n) {
      if (BigInt.asIntN(64, message.asserted_at_ms) !== message.asserted_at_ms) {
        throw new globalThis.Error("value provided for field message.asserted_at_ms of type int64 too large");
      }
      writer.uint32(32).int64(message.asserted_at_ms);
    }
    if (message.signature.length !== 0) {
      writer.uint32(42).bytes(message.signature);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServerVouch {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServerVouch();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.peer_key = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.level = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.note = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.asserted_at_ms = reader.int64() as bigint;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.signature = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServerVouch {
    return {
      peer_key: isSet(object.peerKey)
        ? bytesFromBase64(object.peerKey)
        : isSet(object.peer_key)
        ? bytesFromBase64(object.peer_key)
        : new Uint8Array(0),
      level: isSet(object.level) ? trustLevelFromJSON(object.level) : 0,
      note: isSet(object.note) ? globalThis.String(object.note) : "",
      asserted_at_ms: isSet(object.assertedAtMs)
        ? BigInt(object.assertedAtMs)
        : isSet(object.asserted_at_ms)
        ? BigInt(object.asserted_at_ms)
        : 0n,
      signature: isSet(object.signature) ? bytesFromBase64(object.signature) : new Uint8Array(0),
    };
  },

  toJSON(message: ServerVouch): unknown {
    const obj: any = {};
    if (message.peer_key.length !== 0) {
      obj.peerKey = base64FromBytes(message.peer_key);
    }
    if (message.level !== 0) {
      obj.level = trustLevelToJSON(message.level);
    }
    if (message.note !== "") {
      obj.note = message.note;
    }
    if (message.asserted_at_ms !== 0n) {
      obj.assertedAtMs = message.asserted_at_ms.toString();
    }
    if (message.signature.length !== 0) {
      obj.signature = base64FromBytes(message.signature);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ServerVouch>, I>>(base?: I): ServerVouch {
    return ServerVouch.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ServerVouch>, I>>(object: I): ServerVouch {
    const message = createBaseServerVouch();
    message.peer_key = object.peer_key ?? new Uint8Array(0);
    message.level = object.level ?? 0;
    message.note = object.note ?? "";
    message.asserted_at_ms = (object.asserted_at_ms !== undefined && object.asserted_at_ms !== null)
      ? BigInt(object.asserted_at_ms)
      : 0n;
    message.signature = object.signature ?? new Uint8Array(0);
    return message;
  },
};

function createBaseTreeHeadExchange(): TreeHeadExchange {
  return { server_key: new Uint8Array(0), sth: undefined, observed: [], signature: new Uint8Array(0) };
}

export const TreeHeadExchange: MessageFns<TreeHeadExchange> = {
  encode(message: TreeHeadExchange, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.server_key.length !== 0) {
      writer.uint32(10).bytes(message.server_key);
    }
    if (message.sth !== undefined) {
      SignedTreeHead.encode(message.sth, writer.uint32(18).fork()).join();
    }
    for (const v of message.observed) {
      PeerObservation.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.signature.length !== 0) {
      writer.uint32(34).bytes(message.signature);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TreeHeadExchange {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTreeHeadExchange();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.server_key = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sth = SignedTreeHead.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.observed.push(PeerObservation.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.signature = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TreeHeadExchange {
    return {
      server_key: isSet(object.serverKey)
        ? bytesFromBase64(object.serverKey)
        : isSet(object.server_key)
        ? bytesFromBase64(object.server_key)
        : new Uint8Array(0),
      sth: isSet(object.sth) ? SignedTreeHead.fromJSON(object.sth) : undefined,
      observed: globalThis.Array.isArray(object?.observed)
        ? object.observed.map((e: any) => PeerObservation.fromJSON(e))
        : [],
      signature: isSet(object.signature) ? bytesFromBase64(object.signature) : new Uint8Array(0),
    };
  },

  toJSON(message: TreeHeadExchange): unknown {
    const obj: any = {};
    if (message.server_key.length !== 0) {
      obj.serverKey = base64FromBytes(message.server_key);
    }
    if (message.sth !== undefined) {
      obj.sth = SignedTreeHead.toJSON(message.sth);
    }
    if (message.observed?.length) {
      obj.observed = message.observed.map((e) => PeerObservation.toJSON(e));
    }
    if (message.signature.length !== 0) {
      obj.signature = base64FromBytes(message.signature);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TreeHeadExchange>, I>>(base?: I): TreeHeadExchange {
    return TreeHeadExchange.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TreeHeadExchange>, I>>(object: I): TreeHeadExchange {
    const message = createBaseTreeHeadExchange();
    message.server_key = object.server_key ?? new Uint8Array(0);
    message.sth = (object.sth !== undefined && object.sth !== null)
      ? SignedTreeHead.fromPartial(object.sth)
      : undefined;
    message.observed = object.observed?.map((e) => PeerObservation.fromPartial(e)) || [];
    message.signature = object.signature ?? new Uint8Array(0);
    return message;
  },
};

function createBasePeerObservation(): PeerObservation {
  return { peer_key: new Uint8Array(0), sth: undefined, observed_at_ms: 0n };
}

export const PeerObservation: MessageFns<PeerObservation> = {
  encode(message: PeerObservation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.peer_key.length !== 0) {
      writer.uint32(10).bytes(message.peer_key);
    }
    if (message.sth !== undefined) {
      SignedTreeHead.encode(message.sth, writer.uint32(18).fork()).join();
    }
    if (message.observed_at_ms !== 0n) {
      if (BigInt.asIntN(64, message.observed_at_ms) !== message.observed_at_ms) {
        throw new globalThis.Error("value provided for field message.observed_at_ms of type int64 too large");
      }
      writer.uint32(24).int64(message.observed_at_ms);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PeerObservation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePeerObservation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.peer_key = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sth = SignedTreeHead.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.observed_at_ms = reader.int64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PeerObservation {
    return {
      peer_key: isSet(object.peerKey)
        ? bytesFromBase64(object.peerKey)
        : isSet(object.peer_key)
        ? bytesFromBase64(object.peer_key)
        : new Uint8Array(0),
      sth: isSet(object.sth) ? SignedTreeHead.fromJSON(object.sth) : undefined,
      observed_at_ms: isSet(object.observedAtMs)
        ? BigInt(object.observedAtMs)
        : isSet(object.observed_at_ms)
        ? BigInt(object.observed_at_ms)
        : 0n,
    };
  },

  toJSON(message: PeerObservation): unknown {
    const obj: any = {};
    if (message.peer_key.length !== 0) {
      obj.peerKey = base64FromBytes(message.peer_key);
    }
    if (message.sth !== undefined) {
      obj.sth = SignedTreeHead.toJSON(message.sth);
    }
    if (message.observed_at_ms !== 0n) {
      obj.observedAtMs = message.observed_at_ms.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PeerObservation>, I>>(base?: I): PeerObservation {
    return PeerObservation.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PeerObservation>, I>>(object: I): PeerObservation {
    const message = createBasePeerObservation();
    message.peer_key = object.peer_key ?? new Uint8Array(0);
    message.sth = (object.sth !== undefined && object.sth !== null)
      ? SignedTreeHead.fromPartial(object.sth)
      : undefined;
    message.observed_at_ms = (object.observed_at_ms !== undefined && object.observed_at_ms !== null)
      ? BigInt(object.observed_at_ms)
      : 0n;
    return message;
  },
};

function createBaseDirectoryExchange(): DirectoryExchange {
  return { peers: [], generated_at_ms: 0n, signature: new Uint8Array(0) };
}

export const DirectoryExchange: MessageFns<DirectoryExchange> = {
  encode(message: DirectoryExchange, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.peers) {
      PeerRecord.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.generated_at_ms !== 0n) {
      if (BigInt.asIntN(64, message.generated_at_ms) !== message.generated_at_ms) {
        throw new globalThis.Error("value provided for field message.generated_at_ms of type int64 too large");
      }
      writer.uint32(16).int64(message.generated_at_ms);
    }
    if (message.signature.length !== 0) {
      writer.uint32(26).bytes(message.signature);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DirectoryExchange {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDirectoryExchange();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.peers.push(PeerRecord.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.generated_at_ms = reader.int64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.signature = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DirectoryExchange {
    return {
      peers: globalThis.Array.isArray(object?.peers) ? object.peers.map((e: any) => PeerRecord.fromJSON(e)) : [],
      generated_at_ms: isSet(object.generatedAtMs)
        ? BigInt(object.generatedAtMs)
        : isSet(object.generated_at_ms)
        ? BigInt(object.generated_at_ms)
        : 0n,
      signature: isSet(object.signature) ? bytesFromBase64(object.signature) : new Uint8Array(0),
    };
  },

  toJSON(message: DirectoryExchange): unknown {
    const obj: any = {};
    if (message.peers?.length) {
      obj.peers = message.peers.map((e) => PeerRecord.toJSON(e));
    }
    if (message.generated_at_ms !== 0n) {
      obj.generatedAtMs = message.generated_at_ms.toString();
    }
    if (message.signature.length !== 0) {
      obj.signature = base64FromBytes(message.signature);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DirectoryExchange>, I>>(base?: I): DirectoryExchange {
    return DirectoryExchange.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DirectoryExchange>, I>>(object: I): DirectoryExchange {
    const message = createBaseDirectoryExchange();
    message.peers = object.peers?.map((e) => PeerRecord.fromPartial(e)) || [];
    message.generated_at_ms = (object.generated_at_ms !== undefined && object.generated_at_ms !== null)
      ? BigInt(object.generated_at_ms)
      : 0n;
    message.signature = object.signature ?? new Uint8Array(0);
    return message;
  },
};

export type FederationDefinition = typeof FederationDefinition;
export const FederationDefinition = {
  name: "Federation",
  fullName: "jagoo.v1.Federation",
  methods: {
    /** Mutual key exchange and capability advertisement. TOFU on first contact (FG-01). */
    announce: {
      name: "Announce",
      requestType: AnnounceRequest as typeof AnnounceRequest,
      requestStream: false,
      responseType: AnnounceResponse as typeof AnnounceResponse,
      responseStream: false,
      options: {},
    },
    /**
     * Peer pushes to us, with flow control. Client-streaming, so a NATed node can use
     * this with NO inbound port at all (FD-11).
     */
    deliver: {
      name: "Deliver",
      requestType: Envelope as typeof Envelope,
      requestStream: true,
      responseType: DeliverAck as typeof DeliverAck,
      responseStream: false,
      options: {},
    },
    /**
     * We pull live updates from a peer. Long-lived server-streaming, caller-initiated —
     * the other half of what makes outbound-only federation work.
     */
    streamActivities: {
      name: "StreamActivities",
      requestType: StreamRequest as typeof StreamRequest,
      requestStream: false,
      responseType: Envelope as typeof Envelope,
      responseStream: true,
      options: {},
    },
    /** Catch-up after a partition. Resumable by log index (FG-04). */
    backfill: {
      name: "Backfill",
      requestType: BackfillRequest as typeof BackfillRequest,
      requestStream: false,
      responseType: Envelope as typeof Envelope,
      responseStream: true,
      options: {},
    },
    /** Transparency gossip — detects a peer forking its own log (FG-08, TL-05). */
    exchangeTreeHeads: {
      name: "ExchangeTreeHeads",
      requestType: TreeHeadExchange as typeof TreeHeadExchange,
      requestStream: false,
      responseType: TreeHeadExchange as typeof TreeHeadExchange,
      responseStream: false,
      options: {},
    },
    /** Endpoint and reachability refresh. Used heavily in P3 (ISP bridging). */
    exchangeDirectory: {
      name: "ExchangeDirectory",
      requestType: DirectoryExchange as typeof DirectoryExchange,
      requestStream: false,
      responseType: DirectoryExchange as typeof DirectoryExchange,
      responseStream: false,
      options: {},
    },
  },
} as const;

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

type DeepPartial<T> = T extends bigint ? string | number | bigint
  : T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
