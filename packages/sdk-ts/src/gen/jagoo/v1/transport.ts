// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: jagoo/v1/transport.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Plane, planeFromJSON, planeToJSON, Priority, priorityFromJSON, priorityToJSON } from "./envelope";

/** Ranking, best first: LAN -> ISP_LOCAL -> NATIONAL -> GLOBAL -> MESH -> RETICULUM. */
export enum ReachabilityScope {
  REACHABILITY_SCOPE_UNSPECIFIED = 0,
  /** REACHABILITY_SCOPE_GLOBAL - Reachable from the public internet. */
  REACHABILITY_SCOPE_GLOBAL = 1,
  /** REACHABILITY_SCOPE_NATIONAL - Via domestic peering / IX (e.g. BDIX). */
  REACHABILITY_SCOPE_NATIONAL = 2,
  /** REACHABILITY_SCOPE_ISP_LOCAL - Only within one ISP's AS. */
  REACHABILITY_SCOPE_ISP_LOCAL = 3,
  /** REACHABILITY_SCOPE_LAN - Same local network segment. */
  REACHABILITY_SCOPE_LAN = 4,
  /** REACHABILITY_SCOPE_MESH - WebRTC / BLE peer-to-peer. */
  REACHABILITY_SCOPE_MESH = 5,
  /** REACHABILITY_SCOPE_RETICULUM - RNS destination hash. */
  REACHABILITY_SCOPE_RETICULUM = 6,
}

export function reachabilityScopeFromJSON(object: any): ReachabilityScope {
  switch (object) {
    case 0:
    case "REACHABILITY_SCOPE_UNSPECIFIED":
      return ReachabilityScope.REACHABILITY_SCOPE_UNSPECIFIED;
    case 1:
    case "REACHABILITY_SCOPE_GLOBAL":
      return ReachabilityScope.REACHABILITY_SCOPE_GLOBAL;
    case 2:
    case "REACHABILITY_SCOPE_NATIONAL":
      return ReachabilityScope.REACHABILITY_SCOPE_NATIONAL;
    case 3:
    case "REACHABILITY_SCOPE_ISP_LOCAL":
      return ReachabilityScope.REACHABILITY_SCOPE_ISP_LOCAL;
    case 4:
    case "REACHABILITY_SCOPE_LAN":
      return ReachabilityScope.REACHABILITY_SCOPE_LAN;
    case 5:
    case "REACHABILITY_SCOPE_MESH":
      return ReachabilityScope.REACHABILITY_SCOPE_MESH;
    case 6:
    case "REACHABILITY_SCOPE_RETICULUM":
      return ReachabilityScope.REACHABILITY_SCOPE_RETICULUM;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum ReachabilityScope");
  }
}

export function reachabilityScopeToJSON(object: ReachabilityScope): string {
  switch (object) {
    case ReachabilityScope.REACHABILITY_SCOPE_UNSPECIFIED:
      return "REACHABILITY_SCOPE_UNSPECIFIED";
    case ReachabilityScope.REACHABILITY_SCOPE_GLOBAL:
      return "REACHABILITY_SCOPE_GLOBAL";
    case ReachabilityScope.REACHABILITY_SCOPE_NATIONAL:
      return "REACHABILITY_SCOPE_NATIONAL";
    case ReachabilityScope.REACHABILITY_SCOPE_ISP_LOCAL:
      return "REACHABILITY_SCOPE_ISP_LOCAL";
    case ReachabilityScope.REACHABILITY_SCOPE_LAN:
      return "REACHABILITY_SCOPE_LAN";
    case ReachabilityScope.REACHABILITY_SCOPE_MESH:
      return "REACHABILITY_SCOPE_MESH";
    case ReachabilityScope.REACHABILITY_SCOPE_RETICULUM:
      return "REACHABILITY_SCOPE_RETICULUM";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum ReachabilityScope");
  }
}

/**
 * FD-01: admin allowlisting MUST NOT be the only path to federate. During a shutdown,
 * volunteers stand up relay nodes and cannot wait for manual approval — which is
 * exactly backwards for the threat model. New peers land at PROBATION via TOFU and
 * earn reach through vouches. FD-03: trust affects QUOTA ONLY, never verification.
 */
export enum TrustLevel {
  TRUST_LEVEL_UNSPECIFIED = 0,
  TRUST_LEVEL_BLOCKED = 1,
  /** TRUST_LEVEL_PROBATION - Default on first contact. Class 0-2 only, tight quota. */
  TRUST_LEVEL_PROBATION = 2,
  TRUST_LEVEL_NORMAL = 3,
  /** TRUST_LEVEL_TRUSTED - May relay for others, gossip STHs, act as a bridge. */
  TRUST_LEVEL_TRUSTED = 4,
}

export function trustLevelFromJSON(object: any): TrustLevel {
  switch (object) {
    case 0:
    case "TRUST_LEVEL_UNSPECIFIED":
      return TrustLevel.TRUST_LEVEL_UNSPECIFIED;
    case 1:
    case "TRUST_LEVEL_BLOCKED":
      return TrustLevel.TRUST_LEVEL_BLOCKED;
    case 2:
    case "TRUST_LEVEL_PROBATION":
      return TrustLevel.TRUST_LEVEL_PROBATION;
    case 3:
    case "TRUST_LEVEL_NORMAL":
      return TrustLevel.TRUST_LEVEL_NORMAL;
    case 4:
    case "TRUST_LEVEL_TRUSTED":
      return TrustLevel.TRUST_LEVEL_TRUSTED;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum TrustLevel");
  }
}

export function trustLevelToJSON(object: TrustLevel): string {
  switch (object) {
    case TrustLevel.TRUST_LEVEL_UNSPECIFIED:
      return "TRUST_LEVEL_UNSPECIFIED";
    case TrustLevel.TRUST_LEVEL_BLOCKED:
      return "TRUST_LEVEL_BLOCKED";
    case TrustLevel.TRUST_LEVEL_PROBATION:
      return "TRUST_LEVEL_PROBATION";
    case TrustLevel.TRUST_LEVEL_NORMAL:
      return "TRUST_LEVEL_NORMAL";
    case TrustLevel.TRUST_LEVEL_TRUSTED:
      return "TRUST_LEVEL_TRUSTED";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum TrustLevel");
  }
}

export interface PeerEndpoint {
  /** https:// | grpc:// | rns://<dest_hash> | mesh://<peer_id> */
  uri: string;
  scope: ReachabilityScope;
  /** Autonomous system number — drives the same-ASN bonus (TP-11). */
  asn: number;
  isp_name: string;
  /** Free-form: "dhaka", "chittagong". */
  region: string;
  /** false => this peer must initiate (FD-11). */
  inbound_capable: boolean;
  last_ok_at_ms: bigint;
  rtt_ms: number;
  consecutive_failures: number;
}

/**
 * TP-05: peer records MUST be cached continuously during normal operation, on both
 * nodes and clients. When the gateway drops, the ISP-local addresses must already be
 * known — discovery cannot depend on the network that just failed (THR-11).
 */
export interface PeerRecord {
  /** FD-02: peer identity is the key, never a URL. */
  server_key: Uint8Array;
  display_name: string;
  endpoints: PeerEndpoint[];
  trust: TrustLevel;
  vouched_by: Uint8Array[];
  communities: string[];
  channels: string[];
  /** FED-17: a Signal-only relay is a legitimate deployment. */
  planes: Plane[];
  /** BR-05: multi-homed, relays between islands. */
  is_bridge: boolean;
  bridged_asns: number[];
  last_seen_ms: bigint;
}

/**
 * FD-15: exceeding quota returns backpressure_hint_ms rather than dropping the
 * connection. BR-04: classes 0-2 have reserved capacity that bulk traffic cannot
 * consume — the emergency channel must never be starved by a forum backlog.
 */
export interface Quota {
  envelopes_per_min: number;
  bytes_per_min: bigint;
  max_concurrent_streams: number;
  allowed_classes: Priority[];
}

function createBasePeerEndpoint(): PeerEndpoint {
  return {
    uri: "",
    scope: 0,
    asn: 0,
    isp_name: "",
    region: "",
    inbound_capable: false,
    last_ok_at_ms: 0n,
    rtt_ms: 0,
    consecutive_failures: 0,
  };
}

export const PeerEndpoint: MessageFns<PeerEndpoint> = {
  encode(message: PeerEndpoint, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.uri !== "") {
      writer.uint32(10).string(message.uri);
    }
    if (message.scope !== 0) {
      writer.uint32(16).int32(message.scope);
    }
    if (message.asn !== 0) {
      writer.uint32(24).uint32(message.asn);
    }
    if (message.isp_name !== "") {
      writer.uint32(34).string(message.isp_name);
    }
    if (message.region !== "") {
      writer.uint32(42).string(message.region);
    }
    if (message.inbound_capable !== false) {
      writer.uint32(48).bool(message.inbound_capable);
    }
    if (message.last_ok_at_ms !== 0n) {
      if (BigInt.asIntN(64, message.last_ok_at_ms) !== message.last_ok_at_ms) {
        throw new globalThis.Error("value provided for field message.last_ok_at_ms of type int64 too large");
      }
      writer.uint32(56).int64(message.last_ok_at_ms);
    }
    if (message.rtt_ms !== 0) {
      writer.uint32(64).uint32(message.rtt_ms);
    }
    if (message.consecutive_failures !== 0) {
      writer.uint32(72).uint32(message.consecutive_failures);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PeerEndpoint {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePeerEndpoint();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.uri = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.scope = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.asn = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.isp_name = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.region = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.inbound_capable = reader.bool();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.last_ok_at_ms = reader.int64() as bigint;
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.rtt_ms = reader.uint32();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.consecutive_failures = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PeerEndpoint {
    return {
      uri: isSet(object.uri) ? globalThis.String(object.uri) : "",
      scope: isSet(object.scope) ? reachabilityScopeFromJSON(object.scope) : 0,
      asn: isSet(object.asn) ? globalThis.Number(object.asn) : 0,
      isp_name: isSet(object.ispName)
        ? globalThis.String(object.ispName)
        : isSet(object.isp_name)
        ? globalThis.String(object.isp_name)
        : "",
      region: isSet(object.region) ? globalThis.String(object.region) : "",
      inbound_capable: isSet(object.inboundCapable)
        ? globalThis.Boolean(object.inboundCapable)
        : isSet(object.inbound_capable)
        ? globalThis.Boolean(object.inbound_capable)
        : false,
      last_ok_at_ms: isSet(object.lastOkAtMs)
        ? BigInt(object.lastOkAtMs)
        : isSet(object.last_ok_at_ms)
        ? BigInt(object.last_ok_at_ms)
        : 0n,
      rtt_ms: isSet(object.rttMs)
        ? globalThis.Number(object.rttMs)
        : isSet(object.rtt_ms)
        ? globalThis.Number(object.rtt_ms)
        : 0,
      consecutive_failures: isSet(object.consecutiveFailures)
        ? globalThis.Number(object.consecutiveFailures)
        : isSet(object.consecutive_failures)
        ? globalThis.Number(object.consecutive_failures)
        : 0,
    };
  },

  toJSON(message: PeerEndpoint): unknown {
    const obj: any = {};
    if (message.uri !== "") {
      obj.uri = message.uri;
    }
    if (message.scope !== 0) {
      obj.scope = reachabilityScopeToJSON(message.scope);
    }
    if (message.asn !== 0) {
      obj.asn = Math.round(message.asn);
    }
    if (message.isp_name !== "") {
      obj.ispName = message.isp_name;
    }
    if (message.region !== "") {
      obj.region = message.region;
    }
    if (message.inbound_capable !== false) {
      obj.inboundCapable = message.inbound_capable;
    }
    if (message.last_ok_at_ms !== 0n) {
      obj.lastOkAtMs = message.last_ok_at_ms.toString();
    }
    if (message.rtt_ms !== 0) {
      obj.rttMs = Math.round(message.rtt_ms);
    }
    if (message.consecutive_failures !== 0) {
      obj.consecutiveFailures = Math.round(message.consecutive_failures);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PeerEndpoint>, I>>(base?: I): PeerEndpoint {
    return PeerEndpoint.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PeerEndpoint>, I>>(object: I): PeerEndpoint {
    const message = createBasePeerEndpoint();
    message.uri = object.uri ?? "";
    message.scope = object.scope ?? 0;
    message.asn = object.asn ?? 0;
    message.isp_name = object.isp_name ?? "";
    message.region = object.region ?? "";
    message.inbound_capable = object.inbound_capable ?? false;
    message.last_ok_at_ms = (object.last_ok_at_ms !== undefined && object.last_ok_at_ms !== null)
      ? BigInt(object.last_ok_at_ms)
      : 0n;
    message.rtt_ms = object.rtt_ms ?? 0;
    message.consecutive_failures = object.consecutive_failures ?? 0;
    return message;
  },
};

function createBasePeerRecord(): PeerRecord {
  return {
    server_key: new Uint8Array(0),
    display_name: "",
    endpoints: [],
    trust: 0,
    vouched_by: [],
    communities: [],
    channels: [],
    planes: [],
    is_bridge: false,
    bridged_asns: [],
    last_seen_ms: 0n,
  };
}

export const PeerRecord: MessageFns<PeerRecord> = {
  encode(message: PeerRecord, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.server_key.length !== 0) {
      writer.uint32(10).bytes(message.server_key);
    }
    if (message.display_name !== "") {
      writer.uint32(18).string(message.display_name);
    }
    for (const v of message.endpoints) {
      PeerEndpoint.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.trust !== 0) {
      writer.uint32(32).int32(message.trust);
    }
    for (const v of message.vouched_by) {
      writer.uint32(42).bytes(v!);
    }
    for (const v of message.communities) {
      writer.uint32(50).string(v!);
    }
    for (const v of message.channels) {
      writer.uint32(58).string(v!);
    }
    writer.uint32(66).fork();
    for (const v of message.planes) {
      writer.int32(v);
    }
    writer.join();
    if (message.is_bridge !== false) {
      writer.uint32(72).bool(message.is_bridge);
    }
    writer.uint32(82).fork();
    for (const v of message.bridged_asns) {
      writer.uint32(v);
    }
    writer.join();
    if (message.last_seen_ms !== 0n) {
      if (BigInt.asIntN(64, message.last_seen_ms) !== message.last_seen_ms) {
        throw new globalThis.Error("value provided for field message.last_seen_ms of type int64 too large");
      }
      writer.uint32(88).int64(message.last_seen_ms);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PeerRecord {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePeerRecord();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.server_key = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.display_name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.endpoints.push(PeerEndpoint.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.trust = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.vouched_by.push(reader.bytes());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.communities.push(reader.string());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.channels.push(reader.string());
          continue;
        }
        case 8: {
          if (tag === 64) {
            message.planes.push(reader.int32() as any);

            continue;
          }

          if (tag === 66) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.planes.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.is_bridge = reader.bool();
          continue;
        }
        case 10: {
          if (tag === 80) {
            message.bridged_asns.push(reader.uint32());

            continue;
          }

          if (tag === 82) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.bridged_asns.push(reader.uint32());
            }

            continue;
          }

          break;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.last_seen_ms = reader.int64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PeerRecord {
    return {
      server_key: isSet(object.serverKey)
        ? bytesFromBase64(object.serverKey)
        : isSet(object.server_key)
        ? bytesFromBase64(object.server_key)
        : new Uint8Array(0),
      display_name: isSet(object.displayName)
        ? globalThis.String(object.displayName)
        : isSet(object.display_name)
        ? globalThis.String(object.display_name)
        : "",
      endpoints: globalThis.Array.isArray(object?.endpoints)
        ? object.endpoints.map((e: any) => PeerEndpoint.fromJSON(e))
        : [],
      trust: isSet(object.trust) ? trustLevelFromJSON(object.trust) : 0,
      vouched_by: globalThis.Array.isArray(object?.vouchedBy)
        ? object.vouchedBy.map((e: any) => bytesFromBase64(e))
        : globalThis.Array.isArray(object?.vouched_by)
        ? object.vouched_by.map((e: any) => bytesFromBase64(e))
        : [],
      communities: globalThis.Array.isArray(object?.communities)
        ? object.communities.map((e: any) => globalThis.String(e))
        : [],
      channels: globalThis.Array.isArray(object?.channels) ? object.channels.map((e: any) => globalThis.String(e)) : [],
      planes: globalThis.Array.isArray(object?.planes) ? object.planes.map((e: any) => planeFromJSON(e)) : [],
      is_bridge: isSet(object.isBridge)
        ? globalThis.Boolean(object.isBridge)
        : isSet(object.is_bridge)
        ? globalThis.Boolean(object.is_bridge)
        : false,
      bridged_asns: globalThis.Array.isArray(object?.bridgedAsns)
        ? object.bridgedAsns.map((e: any) => globalThis.Number(e))
        : globalThis.Array.isArray(object?.bridged_asns)
        ? object.bridged_asns.map((e: any) => globalThis.Number(e))
        : [],
      last_seen_ms: isSet(object.lastSeenMs)
        ? BigInt(object.lastSeenMs)
        : isSet(object.last_seen_ms)
        ? BigInt(object.last_seen_ms)
        : 0n,
    };
  },

  toJSON(message: PeerRecord): unknown {
    const obj: any = {};
    if (message.server_key.length !== 0) {
      obj.serverKey = base64FromBytes(message.server_key);
    }
    if (message.display_name !== "") {
      obj.displayName = message.display_name;
    }
    if (message.endpoints?.length) {
      obj.endpoints = message.endpoints.map((e) => PeerEndpoint.toJSON(e));
    }
    if (message.trust !== 0) {
      obj.trust = trustLevelToJSON(message.trust);
    }
    if (message.vouched_by?.length) {
      obj.vouchedBy = message.vouched_by.map((e) => base64FromBytes(e));
    }
    if (message.communities?.length) {
      obj.communities = message.communities;
    }
    if (message.channels?.length) {
      obj.channels = message.channels;
    }
    if (message.planes?.length) {
      obj.planes = message.planes.map((e) => planeToJSON(e));
    }
    if (message.is_bridge !== false) {
      obj.isBridge = message.is_bridge;
    }
    if (message.bridged_asns?.length) {
      obj.bridgedAsns = message.bridged_asns.map((e) => Math.round(e));
    }
    if (message.last_seen_ms !== 0n) {
      obj.lastSeenMs = message.last_seen_ms.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PeerRecord>, I>>(base?: I): PeerRecord {
    return PeerRecord.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PeerRecord>, I>>(object: I): PeerRecord {
    const message = createBasePeerRecord();
    message.server_key = object.server_key ?? new Uint8Array(0);
    message.display_name = object.display_name ?? "";
    message.endpoints = object.endpoints?.map((e) => PeerEndpoint.fromPartial(e)) || [];
    message.trust = object.trust ?? 0;
    message.vouched_by = object.vouched_by?.map((e) => e) || [];
    message.communities = object.communities?.map((e) => e) || [];
    message.channels = object.channels?.map((e) => e) || [];
    message.planes = object.planes?.map((e) => e) || [];
    message.is_bridge = object.is_bridge ?? false;
    message.bridged_asns = object.bridged_asns?.map((e) => e) || [];
    message.last_seen_ms = (object.last_seen_ms !== undefined && object.last_seen_ms !== null)
      ? BigInt(object.last_seen_ms)
      : 0n;
    return message;
  },
};

function createBaseQuota(): Quota {
  return { envelopes_per_min: 0, bytes_per_min: 0n, max_concurrent_streams: 0, allowed_classes: [] };
}

export const Quota: MessageFns<Quota> = {
  encode(message: Quota, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.envelopes_per_min !== 0) {
      writer.uint32(8).uint32(message.envelopes_per_min);
    }
    if (message.bytes_per_min !== 0n) {
      if (BigInt.asUintN(64, message.bytes_per_min) !== message.bytes_per_min) {
        throw new globalThis.Error("value provided for field message.bytes_per_min of type uint64 too large");
      }
      writer.uint32(16).uint64(message.bytes_per_min);
    }
    if (message.max_concurrent_streams !== 0) {
      writer.uint32(24).uint32(message.max_concurrent_streams);
    }
    writer.uint32(34).fork();
    for (const v of message.allowed_classes) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Quota {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQuota();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.envelopes_per_min = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.bytes_per_min = reader.uint64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.max_concurrent_streams = reader.uint32();
          continue;
        }
        case 4: {
          if (tag === 32) {
            message.allowed_classes.push(reader.int32() as any);

            continue;
          }

          if (tag === 34) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.allowed_classes.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Quota {
    return {
      envelopes_per_min: isSet(object.envelopesPerMin)
        ? globalThis.Number(object.envelopesPerMin)
        : isSet(object.envelopes_per_min)
        ? globalThis.Number(object.envelopes_per_min)
        : 0,
      bytes_per_min: isSet(object.bytesPerMin)
        ? BigInt(object.bytesPerMin)
        : isSet(object.bytes_per_min)
        ? BigInt(object.bytes_per_min)
        : 0n,
      max_concurrent_streams: isSet(object.maxConcurrentStreams)
        ? globalThis.Number(object.maxConcurrentStreams)
        : isSet(object.max_concurrent_streams)
        ? globalThis.Number(object.max_concurrent_streams)
        : 0,
      allowed_classes: globalThis.Array.isArray(object?.allowedClasses)
        ? object.allowedClasses.map((e: any) => priorityFromJSON(e))
        : globalThis.Array.isArray(object?.allowed_classes)
        ? object.allowed_classes.map((e: any) => priorityFromJSON(e))
        : [],
    };
  },

  toJSON(message: Quota): unknown {
    const obj: any = {};
    if (message.envelopes_per_min !== 0) {
      obj.envelopesPerMin = Math.round(message.envelopes_per_min);
    }
    if (message.bytes_per_min !== 0n) {
      obj.bytesPerMin = message.bytes_per_min.toString();
    }
    if (message.max_concurrent_streams !== 0) {
      obj.maxConcurrentStreams = Math.round(message.max_concurrent_streams);
    }
    if (message.allowed_classes?.length) {
      obj.allowedClasses = message.allowed_classes.map((e) => priorityToJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Quota>, I>>(base?: I): Quota {
    return Quota.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Quota>, I>>(object: I): Quota {
    const message = createBaseQuota();
    message.envelopes_per_min = object.envelopes_per_min ?? 0;
    message.bytes_per_min = (object.bytes_per_min !== undefined && object.bytes_per_min !== null)
      ? BigInt(object.bytes_per_min)
      : 0n;
    message.max_concurrent_streams = object.max_concurrent_streams ?? 0;
    message.allowed_classes = object.allowed_classes?.map((e) => e) || [];
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

type DeepPartial<T> = T extends bigint ? string | number | bigint
  : T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
